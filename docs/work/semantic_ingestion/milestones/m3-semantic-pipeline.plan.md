# Candidate-To-Terminal Semantic Pipeline Milestone

- Parent WorkPlan: `docs/work/semantic_ingestion/implementation.plan.md`
- Status: active
- Requirements: SIA-R02, SIA-R04, SIA-R05, SIA-R06, SIA-R07, SIA-R09, SIA-R12
- Historical authority: archive headings `M3 - Candidate-to-terminal semantic pipeline`, the M3 closure increments, `M3 Administrative Completion`, and `Post-completion hosted-CI remediation`

## Objective

Turn model proposals into accepted or evidence-only terminal groups through
independent source analysis, closed temporal/trust policy, prompt and egress
authority, lifecycle validation, and M2 atomic persistence. Every ambiguous,
failed, or unauthorized path must have zero graph effect.

## Scope And Owners

Own proposal and source-analysis boundaries, reconciliation, role/scope/time
contracts, prompt authority, egress governance, transaction planning, immutable
attempt/plan/authorization/terminal lineage, and normal-root composition.
Exclude externally approved capability activation, statistical threshold
selection, and M4 full replay/history.

## Completion Evidence

- Candidate, contract, consensus, temporal, prompt, egress, lifecycle, retry,
  restart, redelivery, and exact-authority mutation families pass.
- Public provider, pipeline, persistence, generation, and acceptance selectors
  pass at the recorded dirty revision with exact collection locks.
- Frozen JSONL bytes and complete member maps reopen identically.
- Repository Ruff, configured Pyright, workflow-structure checks, and diff
  hygiene pass.
- Fresh specification, correctness, and test reviews produce empty
  `remaining_validated_p1_p2`, `remaining_blocks_approval`, and
  `remaining_changes_required` arrays.

## Recorded Result

Complete at the exact revision and dirty-tree identities recorded under `M3
Administrative Completion`. A later hosted-CI failure exposed stale test-only
override signatures; the bounded correction and local shard evidence are
recorded under `Post-completion hosted-CI remediation`. Hosted rerun state is
an evidence limitation, not permission to reopen M3 semantics silently.

That historical result is preserved but superseded as a current completion
claim by the explicit 2026-08-05 reopening below.

## Reopened Source And Group Plan Lineage Correction

A direct design-to-production audit established that approved M3 behavior
already required graph-free `SourceDependencyGroup` formation, graph-bound
`TransactionSemanticGroupPlan` expansion, append-only
`SourceTransactionPlanLineage`, and terminal-result equality with the exact
attempt, plan, and `GroupPlanningAuthorization`. Production instead persisted
one opaque plan and one group-result digest for a source.

This is an implementation gap plus an evidence/review-scope gap, not a design
gap. Earlier reviews proved the narrower implemented closure and did not
reconstruct the approved multi-group contract. M3 remains open until the exact
contracts, atomic lineage, restart/replan behavior, terminal binding, and
family-complete evidence exist.

The correction order is strict contracts/codecs; append-only atomic lineage;
exact result/finalization binding; restart, lost-ack, partial-commit, and
graph-change replan proof; then frozen independent review. Test-gate topology
is owned by
`docs/work/semantic_ingestion/persistence-verification-topology/testing.plan.md`.

## Superseded Next Action (2026-08-05)

Complete the determinate Slice B remediation and independent evidence, then
resolve the one missing persisted `SemanticScopePolicy.policy_fingerprint`
domain/preimage decision before freezing the consensus-policy authority slice.

## Normalized Proposal Contract Slice Accepted (2026-08-05)

- Implemented the complete strict provider-to-normalized proposal algebra,
  content-addressed retained/projection/segment artifacts and spans, both text
  mapping proof variants, exact source references, all normalized operation
  members and selectors, complete `SemanticProposal`, and the proposal-bound
  five-kind pre-alignment operation-subject set.
- Normalization now resolves provider-local references deterministically,
  requires immutable projection-byte quote verification, binds the exact
  language route to the segment-local artifact, preserves assertion-local
  containment, rejects operation-ID collisions, and exposes no provider-local
  identifiers in normalized bytes.
- The independently authored complete proposal fixture reproduces canonical CTV
  bytes and the proposal digest without production encoding helpers. It covers
  entity and literal objects, both mapping-proof variants, every proposal
  digest preimage field, map-order equivalence, and semantic tuple ordering.
- Coordinator evidence: 57 normalized-proposal/vector tests passed in 89.88
  seconds; 19 existing source/group/lineage tests passed in 3.44 seconds;
  focused Ruff, `py_compile`, and `git diff --check` passed.
- Accepted production hashes: contracts
  `80e22b7e35dd40a572c4682898d4ce969232661de0753df3d00ebad5362950d1`,
  adapter `7d5021bfd73e293782049f3c297666b241488e00456956c305193abb4f68ea0d`,
  exports `84b78b11c0328d50c594819281aab49fdee4833cca2d02ec3bd488731e275567`.
  Accepted test/fixture hashes: adapter tests
  `6395580b336b72e8ad75503d6a3a0963452f3ef81811f919868eb41c3fa86995`,
  vector tests `261f7725409c6bb56fd46d67bd214ec944cfa85de4b0fa15e0e63159c8070ea9`,
  fixture `f10277f5ab01d74bede98bf031f1e136b72bb479fcc303bb1343c4f353fc6ee0`.
- Three independent final reviewers verified the frozen identities and reported
  no remaining confirmed specification, correctness, or verification finding.
  Pipeline reachability remains intentionally outside this contract-foundation
  slice and is required by the later integration slice.

## Consensus-Policy Slice Design Gap (2026-08-05)

- Production lacked the design-assigned canonical language-policy module and
  all minimal consensus selection/language-construction authority contracts.
  The initial Slice B candidate added those owners and passed its small focused
  suite, but early independent review correctly rejected it as insufficient.
- Confirmed determinate corrections are direct strict codecs for every concrete
  key/leaf, required-nullable wire fields, encoder-side revalidation, exact
  parser/scope authority pairing, canonical map semantics, and family-complete
  independent vectors/mutations. Request-level selection/authority/recovery
  bijection remains owned by the later source-normalization publication slice.
- Confirmed design ambiguity: the approved design declares
  `SemanticScopePolicy.policy_fingerprint` and requires independent fingerprint
  vectors, but does not publish its digest domain, excluded field, declaration-
  order preimage, or collection canonicalization. The implementation candidate
  uses `memorii.semantic-ingestion.semantic-scope-policy.v1`; it cannot become
  approved persisted authority until that exact rule is externally approved
  and recorded in the design.

## Progress: Source Alignment Contract Foundation (2026-08-05)

- Added closed, strict, codec-registered graph-free source-alignment carriers:
  segment language routes/resources, operation alignment, scope and temporal
  consensus references, source-local mention partition, predicate coverage,
  and `SourceProposalAlignment`.
- `SourceDependencyGroup.group_id` is now a content-addressed source-only
  identity; alignment validation proves exact operation/group coverage,
  route/consensus linkage, canonical ordering, and no graph fields.
- Focused evidence: `PYTHONPATH=memorii .venv/bin/python -m pytest
  memorii/tests/unit/core/semantic_ingestion/test_source_group_plan_contracts.py
  -p no:cacheprovider` (5 passed); focused Ruff, `py_compile`, and `git diff
  --check` passed.
- Historical remaining action: wire this sealed source alignment into the graph-bound
  transaction-plan expansion and append-only lineage owners; this contract
  slice intentionally does not implement graph planning or persistence.

## Contract Foundation Review Candidate (2026-08-05)

- Scope: semantic ingestion contract module, package exports, and the three
  plan/source-group/execution-lineage focused test files.
- Git HEAD: `2a7a55e2f1ea265a5c7f824db1a38ce07cd9fb93`; tree is dirty-local.
- Git-status SHA-256: `de3b796316dc13eab07131d2532cbe67adf7bea8e0c383e2d154cc6e55ca01b2`.
- Scoped binary-diff SHA-256: `f1c20ca9cc150ddecfd53ae37ca708649b2acdd4b64cacdb6eb3ca6f7075442a`.
- File SHA-256 values: contracts `4bf3028b352551529c3b69179108b752a1b4576924989661ebecd39a9ec122c7`,
  exports `3a0d886694b34db8c0db9e79301355f65f87fcc9f76f24c77449426e89109b5b`,
  tests `f01346de1ef82003cfc2a3eb20b02e1795d952235d6a884cf45bffb8de2d7e6e`,
  `e86b3b8f7909f44c2ccb1be532ad12b5f9a8d4b3533d9acfc167037302430c8f`,
  and `5a266cd16ab923ab1c40d74eaa64ce24cd74efce4ed967adc106c7c599b6c8ac`.
- Coordinator reproduction: 23 focused tests passed in 3.00 seconds; focused
  Ruff, `py_compile`, and `git diff --check` passed.
- Freeze: no writer may change the scoped files until the bounded contract
  review is reconciled.

## Contract Foundation Review (2026-08-05)

The three frozen reviewers verified all recorded hashes and independently
reproduced the 23-test focused result. Coordinator dispositions:

- Confirmed P2 / `changes_required`: parser consensus can be substituted across
  source, segment, proposal, and route boundaries.
- Confirmed P2 / `changes_required`: predicate coverage accepts noncanonical or
  empty covered references and cannot prove complete inventory membership.
- Confirmed P2 / `changes_required`: transaction plans accept duplicate group
  and execution IDs and group carriers are not an exact operation-derived union.
- Confirmed P2 / `changes_required`: execution manifests check stage presence
  but not required dependency outcomes or exact causal blockers.
- Confirmed P2 / `changes_required`: planned lineage permits a null planning
  authorization, and pre-planning progress does not prove manifest-derived
  dependency closure.
- Confirmed `Not applicable` / `changes_required` verification gaps: aggregate
  lineage/progress/manifest scenarios, family-wide strict wire mutations, and
  independent fixed vectors are absent.
- Confirmed `Not applicable` / `blocks_approval` design ambiguity:
  `SourceProposalAlignment` references `OperationAlignment`, but the approved
  design never defines that persisted type's exact fields or digest preimage.

The determinate findings enter one bounded contract remediation. The
`OperationAlignment` schema requires a linked design decision before contract
foundation approval; current provisional bytes do not establish authority.

Determinate remediation now enforces source-bound parser coordinates, coverage
digests and inventory membership, exact operation carrier unions, unique plan
group IDs, non-null plan-bound lineage authorization, execution dependency and
causal-blocker closure, and manifest-bound pre-planning progress. Twenty-seven
focused tests plus Ruff, `py_compile`, and diff checks pass. Aggregate two-group
replan and fully populated manifest/progress fixtures remain required evidence;
they are test-only work and have no additional design blocker.

The aggregate evidence is now implemented. A two-group replan preserves the
committed group's original final authorization while superseding only the
unfinished group; fork, cycle, orphan, stale-final, cross-group, cross-
repository, manifest dependency, progress-kind, lease, blocker, and reusable-
artifact mutations reject. Coordinator reproduction passed 35 focused tests in
10.30 seconds with no xfail/skip markers; focused Ruff, `py_compile`, and diff
checks pass. The only current semantic blocker is the request-owned consensus-
policy selection decision recorded in the linked design WorkPlan.

## Dependency On M4

M3 emits closed terminal and event-input carriers. It does not claim genesis,
checkpoint, history reconstruction, equal-version conflict, or identity-lineage
replay; those remain M4 obligations.

## 2026-08-09 Focused Reopen Note

## 2026-08-10 Linked Recovery-Renewal Debug Boundary

- The public V3 source-normalization root deterministically stops during the
  second sequential recovery renewal. Detailed hypotheses, changed surface,
  and proof obligations are owned by the linked debugging WorkPlan
  `docs/work/semantic_ingestion/bootstrap-v3-recovery-renewal-2026-08-10/debug.plan.md`.
- M3 remains active. This packet owns only the dependency boundary; it does
  not claim the V3 root/reload acceptance criterion complete until that debug
  operation records passing focused evidence.
- Next action: debug the atomic renewal CAS and admission successor invariant.

- The scenario-only two-segment owner ambiguity remained a bounded M3
  correction. Focused fixes now bind second-child assertion spans to the
  matched quote, canonicalize local terminal candidates before rejected-path
  emission, and classify the protected ambiguity by authenticated source order
  rather than candidate-id order.
- Focused proof passed in `12.68s` across three targeted tests:
  second-segment span binding, protected ambiguity under reversed candidate-id
  order, and rejected local-path canonical ordering.
- The remaining live gap is not a semantic misclassification. The corrected
  direct public ambiguity event reaches terminal persistence and then blocks in
  replay-authority terminal decode. That runtime cost is parked under
  `docs/work/semantic_ingestion/terminal-persistence-performance-2026-08-09/testing.plan.md`
  and is not evidence that the protected ambiguity contract is semantically
  wrong.

## Slice B Frozen Candidate (2026-08-05)

- Implemented the approved minimal consensus-rule witnesses, exact ten-field
  selections and bundle, all five operation semantic-policy keys, complete
  parser/scope language-policy authorities, and direct strict codecs.
- Implemented the approved `SemanticScopePolicy.policy_fingerprint` rule with
  exact frozenset CTV tags. The shared CTV decoder now round-trips maps inside
  sets and frozensets through recursively immutable, hash-stable containers;
  ordinary decoded maps remain ordinary mutable dictionaries.
- Frozen production hashes: ingestion CTV
  `901288306cb79b59a2908871e6950a895830c24afaa5bcde0c01f3c14ce49053`,
  semantic policies
  `3ae52e11194fa6b791823e6d26c3b568eabf40c642bd193ebe25bc8b4a6e58d8`,
  semantic contracts
  `e0129c2e1b70b58fc44526d31ff0869de12c23221b71088c59854f0f42433b1b`,
  and exports
  `133b010346d5627ff16624c8a16a15ceffc12f1b2e7af6bc47f7878fe1acd6ff`.
- Frozen focused-test hashes: CTV regression
  `34109509658791f742ef1c177dd8dd5139934d06c6085c9036853b3d18120190`
  and Slice B
  `e461d2a2f0108621a18f897f0b272829539db688998d017a929f17a90241478b`.
- Coordinator evidence: all 142 selected Slice A, Slice B, source/group-plan,
  and lineage tests passed inside a 400-test run. Of the reference-compiler
  tests, 199 passed and 61 failed from one expected generated-authority
  mismatch after the approved design changed; the compiler itself accepts the
  corrected declarative design. The authority artifact is regenerated once at
  the final design freeze rather than repeatedly during implementation.
- Focused CTV plus Slice B evidence is 51 passed in 6.27 seconds; Ruff,
  `py_compile`, and `git diff --check` pass.
- Final frozen specification, correctness, and test reviewers verified the
  exact candidate hashes, independently reproduced the 51-test result, and
  reported no remaining confirmed finding. Slice B is accepted.

## Historical Next Action (superseded 2026-08-08)

Complete the remaining atomic canonical consensus-contract migration for the
strict-v1 preparation/catalog/request/Step-4 slice at canonical design SHA-256
`495e3c5cd95ca68eb2f3bca5c47870092c148d785fc57688127e9802ba93ddae`.
Request/run/route/literal remediations and the direct-codec/Unicode evidence
slice are implemented. Replace the incompatible legacy parser/scope/temporal
consensus schemas and migrate their analyzer/pipeline callers without invented
span or preparation authority, then run bounded delta reviews on the refrozen
candidate.

Coordinator verification on 2026-08-05 exposed that the first production
contract pass propagated the preparation coordinate only through prepared
source, proposal request, attempt identity, normalized proposal, proposal run,
and `SegmentAnalysisInput`. The declared Step-4 outputs and downstream
consensus/NLI/normalization owners still omitted the required coordinate, and
the existing fixed `SemanticProposal` vector still encoded the old route and
proposal shapes. This is a confirmed implementation gap, not a new design
decision; strict decoding correctly rejected the stale bytes. The initial
focused run therefore recorded `120 passed, 2 failed`, both in the stale fixed
proposal-vector family. No weaker decoder or default is permitted.

The independent two-parent/three-child vector then exposed a second confirmed
implementation contradiction. `SemanticProjectionSegment` currently binds its
projection span to a parent-local projection artifact even though the approved
design defines every projection span in the one source-wide
`SourceSemanticTextProjection` artifact. That prevents the aggregate from
proving source order and makes a same-parent C0/C1 owned-window swap
indistinguishable. The determinate correction is to bind parent projection
spans to the source-wide artifact, retain parent-local segment artifacts, and
require each parent's declared children to form a source-ordered contiguous
partition of that parent's projection span. Declared separators remain only
between parents. This is implementation remediation under the approved design,
not a new semantic decision.

The completed residual vector pass added the independently authored
P0-to-{C0,C1}, P1-to-{C2} topology, 36 request closed-wire/authority mutations,
and regenerated the strict `SemanticProposal` fixture. Terminal evidence is
`77 passed, 1 xfailed`; the sole xfail is the confirmed source-wide
projection/child-partition defect and is not accepted debt. The active
production remediation must remove it and propagate the preparation coordinate
through every existing downstream owner before candidate freeze.

The first production remediation removed that xfail and passed the combined
`162`-test contract family, but coordinator inspection found one overconstraint
before freeze: it required every child context window to equal its complete
parent. The approved algorithm permits a smaller whole-sentence context inside
the parent. The bounded correction must instead bind the child's exact mapping
proof and relative projection/local coordinates while allowing a contained
sub-parent context; a direct valid regression and mismatch mutations are
required before review.

## Predicate Catalog And Parent/Child Candidate Delta (2026-08-05)

- User-approved Decision A is now a candidate design contract:
  `PredicatePromptContract` and capability-bound `PredicateProposalCatalog`
  are strict closed wires with declared CTV domains and release-time
  `PredicatePolicy`/`PredicateSemanticPolicy` projection. The request owns one
  catalog rather than a bare predicate tuple; the capability/catalog/prompt/
  proposer/route equality chain is explicit and live registry lookup is
  forbidden during render, transport, recovery, and replay.
- User-approved Decision B is now a candidate design contract:
  `SegmentLanguageRoute.parent_projection_segment_id` distinguishes the
  execution child from its projection/governance parent. Carriers remain
  immutable parent-keyed; coverage is total child-to-parent UNIQUE-PARENT
  membership, not a child bijection. Unsplit parent==child is valid; cross-
  parent and sibling-window/artifact substitution reject.
- The verification matrix now requires strict codecs, regenerated strict-v1
  vectors, persistence-byte replay under fail-fast sentinels, old-byte
  rejection without upcast, and unshipped rollback removal. Evidence maturity
  is specified only; production and tests are intentionally unchanged pending
  independent review.

## Frozen Review Remediation (2026-08-05)

- The candidate now adds trailing request content identity, runtime-independent
  schema-manifest fingerprints for both catalogs, and exact request decode/
  recanonicalization through attempt/proposal/replay.
- Child run members are explicitly bijective only by execution child; immutable
  governance/admission carriers are uniquely parent-keyed under a total,
  surjective child-to-parent mapping.
- Step-4 event, temporal, and copied consensus/attachment spans are now
  `SourceSpanReference` values, closing parent/artifact/proof/text joins and
  sibling substitution. Required evidence adds real two-predicate catalog
  replay and the `P0->{C0,C1}`, `P1->{C2}` topology family.
- These are candidate design corrections only. Production, codecs, and vectors
  remain unchanged pending refreeze and independent review.

## Strict-V1 Preparation Coordinate Remediation (2026-08-05)

- `SemanticProjectionSegment` now binds a source-wide projection subspan while
  its mapping proof and segment-local artifact remain parent-local.
  `SourceSemanticTextProjection` verifies ordered offsets, exact substrings,
  digests, and separators; `PreparedSource` proves ordered child grouping and
  an exact contiguous partition of every parent span. The former sibling-window
  xfail is a passing rejection.
- Required strict lowercase-hex `preparation_fingerprint` now flows through
  every existing Step-4 lane, linguistic/event/temporal leaf and aggregate,
  parser/scope/temporal consensus, and existing source-normalization evidence
  contracts. Aggregates and source alignment reject cross-preparation joins;
  strict codec mutations reject old bytes with the field absent.
- Focused production hashes: semantic contracts
  `baf6c25e43f0399a6ccb4b4b59596042167888ddca849b3de239a0cd5c04b712`;
  source-normalization contracts
  `37bf3557a041d339d0c183bff24a7e4ad36db59ea9fd7b05108bc6c4c41e8586`.
  Focused test hashes: parent closure
  `fc898eda6544fe4e576b72ea4651a3bd27b45ee483291b62b5a677435ef4c7ca`;
  source analysis
  `e4a73588900db1a6fafba5792bfdaaf90d58504c6809a7d9a962c0caaf09e74a`;
  source-normalization coordinate
  `c62a5e5882f7efacd5c806695cfc652a3a8938e2855ba45060c536dca8a84f5a`.
- Focused parent closure (10), source analysis (39), source-normalization
  coordinate (14), and source-group-plan (5) tests pass. Focused Ruff,
  `py_compile`, prompt-catalog manifest `--self-test`, and `git diff --check`
  pass. Coordinator candidate-freeze evidence records the combined parent,
  request/proposal vector, source-analysis, proposal-run, prompt-catalog, and
  normalized-adapter family at `162 passed in 162.49s`; the final context-window
  correction was then reverified with `49 passed in 45.18s`. Normalization-
  coordinate plus source-group-plan evidence is `19 passed in 3.04s`.
- No concrete NLI contract currently exists. The existing normalization
  evidence owners were propagated; new NLI or source-normalization request
  types remain the next runtime slice, not this remediation.

### Context-Window Correction

- Prepared child ownership still partitions each complete parent projection in
  source order, but a child context may now be a smaller whole-sentence
  subwindow. It must be contained in the parent proof's source-wide projection
  and parent-local segment spans with identical relative coordinates, and it
  must retain the exact parent mapping proof. `SemanticProjectionSegment` now
  joins that proof's projection span to its own source-wide span.
- Parent-closure evidence: 10 tests pass, including a valid multi-sentence
  sub-parent context and proof/relative-coordinate rejection mutations.
  Coordinator terminal evidence for parent closure plus source analysis is
  `49 passed in 45.18s`. Ruff,
  `py_compile`, and `git diff --check` pass. Updated hashes: contracts
  `baf6c25e43f0399a6ccb4b4b59596042167888ddca849b3de239a0cd5c04b712`;
  parent closure `a10b60476b5f979da88ddff913764c4ac3134654f6a1f65cc6701029b3828ea2`.

### Frozen Contract-Slice Review Round (2026-08-05)

### Frozen Review Remediation Progress (2026-08-05)

- Strict request-artifact decode/re-encode closure, parent-keyed proposal-run
  carriers, preparation joins, BCP-47 score-selected routes, closed
  `ClaimValueType` literals, and route-aware prepared/parser/event/temporal
  span checks are implemented locally.
- The prior opaque literal-proposal fixture is invalid under the new exact
  request text closure: its declared 100-scalar context has no checked-in
  plaintext authority. It is superseded strict-v1 test data and must be
  replaced by a clean-room source-text vector with recomputed artifacts,
  spans, proofs, request, and proposal addresses.
- Local proof so far: `py_compile` passed; focused Ruff and `git diff --check`
  passed. The focused test run reached 43 passing before the superseded vector
  was exercised by the strengthened request contract.

- Specification, correctness, and test reviewers independently verified the
  frozen design and production hashes. Coordinator evidence before review was
  `162`, `49`, and `19` passing tests for the recorded families.
- Eight findings are confirmed. Product/runtime P2 corrections are: exact
  request source/context/text/governance closure; canonical strict request
  artifact decoding; parent-keyed split-child run sealing; run-to-attempt and
  run-to-proposal preparation equality; parent-aware `SourceSpanReference`
  joins across prepared annotations and Step 4; unique threshold-certified
  selected language routes; and closed predicate literal domains.
- Two verification corrections are `Not applicable / changes_required`:
  direct strict codec/rollback matrices for preparation and catalog owners, and
  a Unicode-scalar repeated-substring/combining/emoji two-parent topology.
- No finding is duplicate, unsupported, accepted limitation, or externally
  blocked. Runtime builder/executor/persistence and not-yet-existing NLI/source-
  normalization request/result types remain the next implementation slice and
  were not treated as findings in this review.
- Remediation is split at a proven artifact boundary with one sequential writer
  at a time. Direct codec matrices, Unicode-scalar topology, and shared strict
  request test support form one evidence-only slice. Copied-consensus span
  closure is a separate atomic production migration because the approved design
  replaces incompatible legacy consensus schemas and introduces their missing
  canonical owners plus analyzer/pipeline call-site changes. This split changes
  neither scope nor target semantics and requires no external decision.

## Final Candidate Remediation (2026-08-05)

- `CanonicalAttributionBearerBinding`, `TemporalEvidenceCandidate`, and
  `TemporalEvidenceAssessment` now retain `SourceSpanReference` joins through
  stable scope/resolved temporal evidence, including null/empty authenticated
  temporal behavior and P0/P1 sibling-parent rejection through replay.
- The checked-in `prompt-catalog-schema-manifest-v1.json` plus its deterministic
  validator make both catalog schema fingerprints reproducible without runtime
  introspection. Artifact digest:
  `d5c4cf1386f26f1cbb9140b24c5e466553794afaafe41d6f1dc9626aea1b1237`.
- The required proof is now literal about rendered entity/literal catalog bytes,
  poisoned-registry zero calls, and a clean-room P0/P1 preparation-fingerprint
  CTV oracle. This remains specified evidence awaiting implementation and
  refrozen independent review.

## Whole-Design Candidate Remediation (2026-08-05)

- The global post-Step-2 source-span rule now closes NLI, check-result,
  attachment, and certified-time evidence with structural-only projection-span
  exceptions. All converted paths carry P0/P1 sibling-parent replay rejection.
- `PreparedSource` has a complete policy-bound content address and all proposal,
  analysis, consensus, NLI, run, and normalization carriers copy it exactly.
  The P0/P1 oracle recomputes its full CTV preimage and changes on one
  child-parent coordinate mutation.
- The catalog manifest now has distinct literal-domain tokens and the validator
  enforces exact record mappings plus per-token self-test mutations. The
  candidate remains design-only and requires refreeze/re-review.

## DREV-024 Candidate Remediation (2026-08-05)

- `TextPreparationPolicy` replaces the opaque preparation policy fingerprint.
  It is a strict persisted v1 owner for limits, canonical language support, and
  both fully defined deterministic segmentation/context algorithms.
- Request and prepared-source retain the same policy bytes; the prepared-source
  CTV identity includes that record, and downstream replay rejects stale or
  substituted policy/fingerprint bytes even when segmentation output is equal.
- Codec/rollback/no-output-change mutation evidence is specified only. The sole
  next action remains refreeze and independent review before implementation.

## Active Delegation And Cost Ledger

- 2026-08-05: one Terra `worker` is the sole writer for the coherent central
  attempt plus Step-4 contract/codec/test slice. Spark mapping was attempted
  once and stopped at the exhausted quota; no retry cost was spent. The
  coordinator owns WorkPlan reconciliation and will inspect the diff and run
  focused gates before milestone review.
- 2026-08-05: a bounded Spark read-only mapper was attempted for the following
  persistence slice and stopped immediately at the known quota limit. No retry
  was spent; the accepted repository ownership map is sufficient for the sole
  writer packet.

## Attempt And Step-4 Contract Slice Accepted (2026-08-05)

- Implemented the approved two-record semantic-proposal attempt family,
  complete final-attempt-history run validation, originating-attempt binding,
  Step-4 inputs, manifests, linguistic/event/temporal leaves and aggregates,
  strict codecs, and normalized-proposal adapter propagation.
- Frozen production hashes: contracts
  `e81bfcc3aa03cb0c8f054a9f2d9c828db7943612915fa60ecb4767977f997e7a`,
  exports
  `9bf042a8ce9c4039698a65a893ba16c2acbfebb124a77e410fa36f359ed90e86`,
  proposal adapter
  `5d4254780386607221a63f18c601ddf4c0aad552215665f422da24078a7088dc`.
- Frozen focused test hashes: source analysis
  `2a7e96ec81d14218faff056f8d014e9882e220d4897112508a41cf84e3e1f9e1`,
  proposal run
  `33cc008c3c5021afdc462f862320b565eb005d9af41d50d129f717960b43c75c`,
  normalized adapter
  `dd7276bc8f5c8947321830bcd63a345a85612381315d2fd63ca3eb5c5d7663d8`.
- Coordinator terminal evidence: 39 source-analysis tests, 12 proposal-run
  tests, 68 proposal-vector/Slice-B tests, and 31 normalized-adapter tests
  passed; focused Ruff, `py_compile`, and diff hygiene passed.
- Specification review accepted the strict attempt, route, manifest, and parser
  contracts. Correctness review accepted the exact temporal candidate-to-route
  and artifact-bearing outcome join. Test review accepted the 22-type strict
  codec matrix, all 25 ordered aggregate-status pairs, and complete run-status
  mutation coverage. No confirmed finding remains in this bounded slice.

## Attempt Persistence Integration Prerequisite (2026-08-05)

- The persistence writer proved that the live `SemanticIngestionPipeline.run`
  path is still source-level: it has source identity/text, prompt authority,
  egress binding, and raw transport bytes, but no prepared segments, exact
  governance carriers, owned/context spans, language routes, or complete
  proposer fingerprints.
- The approved `SemanticProposalAttemptIdentity` requires those values before a
  call. Persisting the current ad-hoc source-level attempt would create a weaker
  parallel identity and violate the accepted design. The writer removed its
  exploratory envelope and left no file changes.
- Crash-safe persistence therefore follows, rather than precedes, the canonical
  per-segment preparation/proposal execution integration. This is a dependency
  correction inside M3, not a scope addition or external decision blocker.
- The following execution slice then found two persisted-design blockers: the
  undefined `PredicatePromptContract`/catalog authority and the contradiction
  between permitted child segmentation and validators that require child,
  projection, governance, span, and route segment IDs to be identical. These
  are recorded in the reopened linked design WorkPlan; no weaker implementation
  was retained.

## Normalization Publication Decision Slice Accepted (2026-08-05)

- Implemented the strict graph-free `SourceNormalizationPublicationCoordinate`,
  `SourceNormalizationEvidenceEntry`, and
  `SourceNormalizationEvidenceManifest` in the semantic-analysis decision
  owner, including their exact CTV domains, generation successor, source/fence
  coupling, canonical entry order, and coordinate/artifact/selection bijection.
- Frozen hashes: decision contracts
  `2cc28a1e92c94d5ab67b3199a0af59114355a1582ef5852432a21e1ab347297f`,
  exports
  `2d7e1255a6e09f5287cb4b2d7fdf7644bea7e1e225da5ef228fc47c15dbcf3d8`,
  focused tests
  `5aef6a33067b97716837f7de9b1d4a5db905ec7013b5a5e9360a823d6fd42cf0`.
- Coordinator evidence: 14 focused tests passed; Ruff, `py_compile`, and diff
  hygiene passed. Independent specification and correctness reviewers matched
  the hashes, reproduced the result, and reported no confirmed finding.
- Request/bundle/artifact equality joins and atomic-generation completeness are
  explicitly deferred to the request/result/atomic-write integration slice.

## Attempt Identity Design Blocker (2026-08-05)

- `SegmentProposalOutcome` requires `attempt_digest`, but the approved
  `SemanticProposalAttempt` schema declares no such field and no authoritative
  derivation. Production cannot implement the run bijection without inventing
  persisted identity.
- Approved external decision: add trailing required `attempt_digest` to
  `SemanticProposalAttempt`, using domain
  `memorii.semantic-ingestion.semantic-proposal-attempt.v1` and canonical CTV
  over every other declared attempt field. The outcome copies it byte-for-byte.
- The decision is recorded in the canonical design. Resume the source/proposal-
  run prerequisite without introducing any alternate or compatibility digest.

Independent delta review found that the one-record rule conflicts with the
existing pre-call persistence invariant: response/status fields are unavailable
when attempt identity must first be persisted. The user approved the corrected
two-record boundary, and the reopened design was independently accepted at
SHA-256 `fa32e2246da8188e7899cc033892b9c396a1805761d0d041e4fd10eadf6a4488`.
Implementation is no longer design-blocked.

## Analysis Input Design Gap (2026-08-05)

- The design names `LinguisticToken`, `SourceMention`, `ClauseAnalysis`, and
  `DependencyArc` but does not define their closed fields.
- It declares the analysis/event/temporal aggregate shapes but no authoritative
  CTV domains/preimages for their persisted fingerprints/digests. No canonical
  production owner supplies the missing definitions.
- The linked design operation closed every leaf, aggregate, digest, manifest,
  status, temporal-basis, and replay join and passed independent review at
  design SHA-256
  `fa32e2246da8188e7899cc033892b9c396a1805761d0d041e4fd10eadf6a4488`.
  The analysis-input writer may resume without inventing persisted semantics.

## Consensus Contract Migration In Progress (2026-08-05)

- Replaced the legacy optional-coordinate parser consensus and bare `SourceSpan`
  analyzer records with the strict SourceSpanReference-based role, scope, and
  temporal consensus family in the canonical contracts owner. The new records
  have closed v1 codec registration and CTV addresses.
- The unprepared local analysis boundary now abstains. It has no prepared
  segment, selected route, mapping proof, proposal ID, or operation coordinate;
  it therefore cannot manufacture a source reference or preparation digest.
- Added direct strict-codec proof: 6 consensus contract vector/mutation tests
  pass. The expanded canonical source-alignment family has 7 direct tests; the
  existing source-group family now runs against that real prepared route/proof
  fixture, for 12 passing tests total. `py_compile` and focused Ruff pass for the altered local analyzer,
  pipeline, and operation assessment files.
- The legacy semantic-pipeline test family is not yet migrated. Its focused run
  currently has 12 setup failures from the independently dirty
  `TrustPolicySnapshot.create` digest mismatch before consensus construction,
  plus one expected legacy analyzer-shape failure. Do not treat that run as
  consensus evidence. The next action remains migration of the source-analysis
  and source-alignment callers/tests through the prepared route boundary.

- Residual legacy analyzer constructors were removed from the semantic pipeline,
  provider composition, integration pipeline, and source-group test builders.
  These now collect (86 selected tests) without the removed API. The direct
  canonical source-group/codec/local-abstention selection passes 13 tests.
  A scoped `rg` over the four former builders finds zero old role fields,
  old parser constructor coordinates, or synthetic all-zero preparation values.

## Resolved Design Ambiguity: SemanticScopePolicy.policy_fingerprint (2026-08-07)

The explicit design gap regarding SemanticScopePolicy.policy_fingerprint is closed by the governing architecture definition (docs/design/semantic_ingestion_architecture.md, lines 4140–4165). Implementation must bind to these exact rules:

* **Digest Domain**: The entire model payload serialized as a canonical CTV (Closed-Typed-Value) preimage over every declared field in declaration order.
* **Excluded Field**: policy_fingerprint is the only excluded field; it is written last and does not participate in its own hash preimage.
* **Collection Canonicalization**:
    * Maps (embedding_head_lemmas): Canonical by sorted CTV map key insertion order. Permutations of insertion produce identical bytes.
    * Frozenset fields: Canonicalize by strictly sorted typed-value order.
    * Tuples (`allowed_predicate_ancestor_paths`, `negation_bearer_patterns`,
      `temporal_attachment_patterns`): Must be duplicate-free and ordered by
      the nested `pattern_digest`.
    * Nested UdPathPattern.steps: Preserves declared traversal order and contributes byte-for-byte through the nested pattern_digest.
* **Validation Constraints**: A missing field, inferred default, duplicate or noncanonical pattern tuple, invalid nested digest, map-order-dependent fingerprint, legacy preimage, or unknown field must reject before policy reuse.

## Strict Codec & Validator Requirements (Derived)

1. The serializer must enforce declaration-order traversal without relying on __dict__.items() unless explicitly bound to the model's __annotations__ order.
2. Map keys must be sorted lexicographically during CTV encoding; values are recursively encoded using the same rules.
3. Frozensets must be converted to a list, strictly sorted by their canonical hashable form, then encoded as a fixed-sequence before hashing.
4. The fingerprint is computed exactly once per policy instance and stored immutably; regeneration must yield byte-identical output without re-evaluating the domain.

## Consensus-Policy Authority Slice Unblocking (2026-08-07)

With this ambiguity resolved, the consensus-policy authority slice may proceed to:
1. Implement SemanticScopePolicy codecs using the sorted-map/frozenset/tuple rules above.
2. Regenerate strict-v1 vectors and fixtures that validate the map-order independence and frozenset ordering.
3. Freeze the consensus-policy authority slice for independent review.

## Current M3 Lineage Remediation (2026-08-08)

- Candidate identity: `docs/work/semantic_ingestion/history/m3-lineage-remediation-candidate-identity.json`
  pins HEAD `4691c03` and the exact dirty remediation before any review or
  additional writer work.

- Removed the fabricated `SemanticIngestionPipeline` lineage path: it invented
  governance/admission/scope carriers, repository IDs, digests, group plan,
  authorization, and attempt values. It also incorrectly changed the stable
  `SemanticTerminalOutcome` digest surface and emitted an M4-only manifest
  placeholder.
- Restored the terminal schema and persistence member sets, preserving legacy
  terminal decode/reopen bytes. `SourceTransactionPlanLineage` remains a typed
  contract, but no terminal summary claims it as an attached authority.
- Fixed the policy canonicalizer and added the fixed two-map/two-family vector:
  focused policy, source-group, lineage, and pipeline contracts pass (`76 passed`).
- The terminal test support now supplies the existing prepared independent-
  analysis fixture, and the repaired execution-lineage fixture supplies its
  parent projection segment. The real store path reaches checkpoint, committed
  group, finalization, JSONL reopen, and finalization lost-ack retry while
  preserving the legacy terminal wire and rejecting M4-only terminal fields.
- Blocker: the design requires a coordinator-owned
  `TransactionSemanticGroupPlanRepository` over the atomic store. No production
  implementation, composition-root dependency, or typed `get`/atomic-publication
  boundary exists; the legacy pipeline has no sealed source alignment, graph
  snapshot, repository identity, or planning inputs. Constructing them there
  would recreate fabricated authority.
- Earlier direct blocker: production has no canonical `PreparedSource` producer
  or atomic publication/read boundary. The approved V1 design now names the
  authoritative admitted-record inputs, producer, publication generation, read
  authority, and provider composition mapping. Production remains evidence-only
  until that approved boundary is implemented; it must not fabricate retained,
  projection, or governance authority.

## Historical Next Action (Superseded 2026-08-09)

Implement the approved corrected V1 bootstrap authority at canonical design
SHA-256 `43550572621383259ed31c3dd7942c2e5cf43e0acd4692cd50abefede6afd1bd`,
starting with the admitted-record and source-owned prepared-publication CAS
handoff. Use only the existing V1 coordinate and release machinery, preserve
the linked design's exclusions, and collect the required checker, composition,
failpoint, JSONL, collection, timing, and CI evidence.

## Source-Preparation Implementation Attempt (2026-08-08)

- Replaced the unsafe caller-supplied `PreparedSource` parameter at the
  pipeline boundary with a repository load keyed by exact source ID and
  source digest. A missing, invalid, unpublished, or text-substituted record
  returns `prepared_source_authority_unavailable` before any learned call.
- Added `TextPreparationService` and the `PreparedSourceRepository` protocol
  under `memorii.core.semantic_ingestion.source_preparation`. The configured
  producer is revalidated against the requested identity/text before
  publication; the process-local implementation remains test-only.
- Added a durable atomic-store publication/read owner. It binds an immutable
  prepared-source payload and fingerprint to one source identity and rejects
  source/digest/fingerprint substitution and non-identical republish.
- Runtime composition now carries a paired producer/repository and provider
  ingestion invokes it before the pipeline. The ordinary configured provider
  composition regression reaches its former learned-transport path; an absent
  pair remains evidence-only.
- This is not M3 closure: the design-defined Step-1 `SourceObservation`,
  admitted-record, governance/projection construction, router manifest,
  routing policy, and certified language-resource authority have not yet been
  modeled or wired. The temporary test producer is not an acceptable
  production source ingestor. Do not freeze this candidate.

## Canonical Source-Owner Migration In Progress (2026-08-08)

- Added the required canonical owner modules: `source_admission.py` has the
  closed provider-operation mapper and strict request identity closure;
  `source_governance.py` owns exact complete-scope authorization; the analysis
  source-contract import boundary avoids a second persisted representation; and
  `language_router.py` owns deterministic threshold/margin selection.
- `TextPreparationService` now accepts one `TextPreparationRequest` rather
  than an opaque `(source_id, source_digest, source_text)` producer input.
  This exposes the remaining migration explicitly: the currently imported
  legacy `SourceObservation` does not retain the immutable source digest, and
  `ProviderIngestionCoordinator` still invokes the retired three-string API.
- Static evidence for the new owners and preparation service: focused Ruff,
  `py_compile`, and `git diff --check` passed. The provider/preparation pytest
  shard is not a passing result after the intentional API migration and must
  not be cited as evidence.
- Next action: migrate the durable admission handoff to the design-shaped
  observation (including source digest and atomic admitted carriers), then
  wire provider composition to build and publish `TextPreparationRequest`
  from that exact durable observation before any learned call.

## Delegation And Cost Ledger Update

- 2026-08-08: one Terra writer removed the unsafe M3 patch, repaired the
  prepared-analysis persistence fixture, and added the JSONL legacy-wire
  recovery regression. No delegated work was started. The graph-plan repository
  remains an implementation dependency, not a recoverable test failure.

## Prepared-Analysis Persistence Performance Correction (2026-08-08)

- Replaced the terminal fixture's per-call clean-room
  `IndependentSourceAnalysis` reconstruction with delegation to the production
  local analyzer using the exact `PreparedSource` loaded by the pipeline.
- The production analyzer now accepts only one complete, exact-bound prepared
  source with one selected route, derives strict artifact-backed
  subject/object/predicate spans, parser consensus, and authenticated interval
  candidates, and abstains on missing, substituted, malformed, or unsupported
  authority. A direct positive/negative test passes (`1 passed in 3.06s`).
- Added test-only immutable memoization of identical prepared fixture inputs.
  Every terminal still publishes to and loads from a fresh repository, whose
  clone/revalidation boundary prevents cross-test mutation or state leakage.
- Focused `py_compile`, scoped Ruff, and `git diff --check` pass. The exact
  JSONL accepted-completion lost-ack regression remained active after 60
  seconds on a clean post-change run and was intentionally stopped; it has no
  terminal pass/fail result. The full terminal persistence family was not run,
  and no candidate identity was frozen. The linked
  `docs/work/semantic_ingestion/terminal-persistence-performance-2026-08-09/testing.plan.md`
  owns this performance/topology follow-up; it is not an M3 correctness blocker.

## Persistence-Shard Closure Preflight (2026-08-08)

- CI topology was inspected directly in `.github/workflows/pr-gates.yml` and
  `memorii/tests/ci/semantic-terminal-persistence-shards.json`. The dedicated
  family is seven exact-node shards, with `-W error`, no pytest cache, and one
  timing artifact per shard.
- Current collection is `224` nodes, while the checked-in timing inventory has
  `156`. Its pre-run verifier therefore fails before execution: the 68 unknown
  nodes use the 14.040950-second median and estimate all seven shards at about
  646.8 seconds, above the configured 600-second limit. No node was dropped;
  recapturing all seven timing outputs and merging them remains required once
  the focused production gate is green. The linked testing WorkPlan owns this
  evidence gap and all shard/topology changes.
- The required focused analyzer/pipeline/provider/integration/JSONL selection
  collected 108 tests. It was stopped after the current buffered test at the
  coordinator's request and ended `12 failed, 57 passed in 484.09s`; it is not
  passing evidence. First causal failure is
  `test_real_filesystem_hermes_corruption_recovery_restart_and_racing_write`:
  ordinary `sync_turn` produced no semantic event batch.
- Tracing found `build_authorized_local_semantic_runtime` constructs no
  `TextPreparationService`, `PreparedSourceRepository`, or
  `TextPreparationPolicy`. `ProviderIngestionCoordinator._run_semantic_ingestion`
  consequently returns `evidence_only` with
  `prepared_source_authority_unavailable` before proposal, analysis, or
  terminal persistence. The remaining provider/clarification/recovery failures
  are consistent downstream symptoms.
- Do not repair this by importing the clean-room test producer or accepting a
  caller-supplied `PreparedSource`. The governing source requires the retained
  `SourceObservation` to include exact semantic projection, carrier, and
  context authorities before Step 2 (`semantic_ingestion_architecture.md`
  Sections 4.1.3 and 4.2.2-4.2.3). The current production
  `SourceObservation`/record projection has only legacy text, IDs, timestamps,
  and source digest; its admitted record does not retain the required
  projection/carrier/context inputs. A factory-only wiring would fabricate the
  missing authorities.

## Bootstrap V1 Contract Unit (2026-08-09)

- Implemented the bounded bootstrap-contract owner in
  `memorii/memorii/core/memory_evolution/bootstrap_profile.py`: the V1
  manifest now has the exact eight component owners, embedded frozen
  `TextPreparationPolicy`, literal `en`, and component-root/profile digest
  coverage. The profile builder regenerates those three content artifacts from
  that one manifest shape.
- Added strict typed host release evidence and current-release assertion
  contracts. Profile verification validates the active host evidence and its
  signed-release/anchor bindings before any artifact decode, retains the exact
  evidence in `VerifiedBootstrapProfile`, and includes it in verification
  identity. The existing traceability-release CTV grammar still rejects the
  design-required release-body fields; release-byte/binding regeneration is a
  concrete downstream artifact dependency, not claimed by this unit.
- Focused proof: `PYTHONPATH=memorii .venv/bin/python -m py_compile ...` passed
  for the changed bootstrap/release/test owners; the new pre-decode evidence
  mutation test passed (`1 passed in 18.72s`). A scoped Ruff run found only
  import-order findings in already-dirty shared test-support files after their
  typed material construction was updated; the production bootstrap/release
  owners need a clean scoped rerun after the overlapping writer settles.
- This is not M3 closure. No checker artifact, external signed release bytes,
  admission CAS assertion use, JSONL/failpoint proof, timing inventory, or CI
  job was added in this contract unit.

## Historical Next Action (Superseded 2026-08-09)

Wire `CurrentBootstrapReleaseVerifier` assertions through the source-owned
prepared-publication CAS and writer-handoff boundaries without persisting an
assertion, then add the corresponding memory/JSONL race and reopen tests.

## Real Composition Closure Attempt (2026-08-09)

- The ordinary host-runtime protocol now receives the verified bootstrap
  profile. `build_authorized_local_semantic_runtime` is the canonical owner of
  the atomic prepared-source repository, profile-owned text-preparation
  service, profile policy, local analyzer, and zero-egress pipeline; the
  provider root no longer replaces a capability-returned runtime with an
  injected producer.
- Hermes and filesystem composition fixtures now construct the ordinary local
  runtime through that owner. Explicit remote/transport fixtures remain
  separate, rather than being used to prove the ordinary local route.
- Missing profile, swapped release root, and missing authenticated ingress
  proof is deterministic and graph-free: `PYTHONPATH=memorii .venv/bin/python
  -m pytest -x -q memorii/tests/unit/core/semantic_ingestion/test_semantic_provider_composition.py::test_normal_provider_root_missing_bootstrap_authority_is_evidence_only
  -p no:cacheprovider` completed `3 passed in 22.85s`.
- Scoped `py_compile`, Ruff, and `git diff --check` pass for the capability,
  provider-service, and composition-test owners.
- The canonical accepted selector and real filesystem restart/race selector
  do not yet establish passing evidence. Both crossed a 15-second
  faulthandler threshold and the terminal wrapper's 30-second limit while
  serializing terminal persistence (`SemanticTerminalSeal.validate_seal` /
  `contract_digest` / `_normalized_typed_json`). This is a concrete local
  performance/serialization evidence gap outside composition authority wiring.
  The linked testing WorkPlan owns controlled profiling and remediation; M3
  correctness instead relies on passing long-tier focused evidence.

## V1 Admission/Persistence Unit In Progress (2026-08-09)

- Added the typed `BootstrapAuthenticatedLanguageEvidence` and
  `BootstrapAdmissionPin` contracts. The language proof is derived only from
  authenticated ingress, retained source bytes, stable principal binding, and
  sealed governance-carrier digests; it is retained in the request/observation
  handoff and its digest rejects substitution.
- Added a source-owned prepared-publication CAS carrying the exact pin,
  original release evidence, language proof, ordered grammar-proof/route
  tuple, and prepared generation. It creates no preplanning control, lease,
  writer binding, or pending operation.
- Added typed bootstrap writer-handoff request, marker, result, and access-
  denied contracts. The request excludes current authorization/release
  assertions from its digest; the marker binds the exact prepared tuple,
  V1 pin/release/language evidence, delivery/fence, and writer binding.
- Local evidence: language-evidence retention unit `1 passed in 3.78s`;
  existing atomic-store family `17 passed in 4.54s`; JSONL memory-plane family
  `9 passed in 1.41s`; focused Ruff, `py_compile`, and `git diff --check`
  pass. The broad bootstrap-admission command completed with ten progress
  dots but this terminal wrapper did not report a pytest summary, so it is not
  passing evidence.
- Remaining work: inject and exercise the host current-release verifier at
  both CAS linearization points; persist/reopen the two exact
  authority-unavailable terminal variants; add source-publication and
  handoff lost-ack/concurrency JSONL tests. Provider composition and artifact
  regeneration remain explicitly excluded.

## Historical Next Action (Superseded 2026-08-09)

Finish the current-release verifier/terminal branch and its focused in-memory
and JSONL lost-ack/concurrency proof before treating the V1 admission/
persistence unit as a review candidate.

## V1 Admission/Persistence Closure Evidence (2026-08-09)

- `SemanticIngestionAtomicStore` now owns typed, digest-checked retained-pending
  and prepared-published authority-unavailable terminals. Both retain the exact
  pin/release/language/delivery/fence joins; the prepared form additionally
  retains its generation and prepared-source digest. The closed semantic-control
  classifier and writer policy admit only an isolated exact terminal record.
- The atomic owner accepts an opaque `CurrentBootstrapReleaseVerifier` and
  revalidates current delivery/session/scope authority plus a host assertion at
  prepared publication, pre-handoff recovery, and the handoff write point. No
  assertion is serialized. A pre-lookup failure remains `BootstrapHandoffAccessDenied`;
  a write-point failure persists the corresponding terminal and cannot reopen.
- Focused proof: `PYTHONPATH=memorii .venv/bin/python -m pytest -q
  memorii/tests/unit/core/semantic_ingestion/test_semantic_atomic_store.py`
  completed `22 passed in 4.09s`, including in-memory/JSONL terminal idempotency
  and reopen for both variants plus exact verifier phase invocation. `py_compile`,
  scoped Ruff, and `git diff --check` also passed.
- Residual evidence gap: no provider composition was added. The focused tests do
  not yet exercise a full prepared-source construction followed by the public
  handoff request's lost-ack/concurrent race or its request-digest mutation;
  those remain required before M3 review candidacy.

## Historical Next Action (Superseded 2026-08-09)

Add the complete prepared-publication/handoff request construction and its
lost-ack, competing-call, stale-writer, revocation, and request-mutation race
matrix without wiring provider composition.

## Provider Runtime Wiring Blocker (2026-08-09)

- The requested normal-root wiring cannot truthfully construct a production
  `TextPreparationService` from the current admitted value.  The governing
  Step-1 contract in `docs/design/semantic_ingestion_architecture.md` Sections
  4.1.3 and 4.2.2-4.2.3 requires an admitted `SourceObservation` to retain the
  exact semantic projection, source semantic context, complete segment
  governance and message-admission carrier sets, governance carrier artifact,
  retained-text artifact, and authenticated admission proof.  The live
  `memorii.core.memory_evolution.models.SourceObservation` retains only legacy
  text/metadata plus source and delivery digests and optional bootstrap
  language evidence.  `GovernedSourceAdmissionService` persists no projection,
  context, carrier, artifact, or admission-proof owner.
- The required bootstrap route value is also absent from production contracts:
  there is no `BootstrapDeclaredSegmentLanguageRoute` or
  `PreparedSource.grammar_proofs` field.  The atomic-store CAS can persist
  proofs only after a complete prepared source already exists, so it cannot
  supply the missing producer authority.  The sole complete constructor found
  is `tests/unit/core/semantic_ingestion/clean_room_request_test_support.py`,
  which fabricates the missing retained/projection/governance values and is
  prohibited from production composition.
- Consequently, connecting `build_authorized_local_semantic_runtime` to a
  producer now would either invent all source authority or add a test-only
  injection seam.  Both violate the approved V1 route.  No production source,
  prepared-publication, handoff, or terminal byte was changed by this audit.
- Exact unblock: implement the approved Step-1 admitted-observation persistence
  contract and the typed bootstrap declared-route/grammar-proof contracts,
  including their canonical server-owned derivation inputs.  Then the bounded
  provider-root factory can construct the verified embedded-policy producer,
  publish through the existing CAS, and call the existing handoff without
  fabricating authority.

## Step-1 Route Contract Progress (2026-08-09)

- Implemented the strict production `BootstrapDeclaredSegmentLanguageRoute`
  contract and content-addressed codec registration. `PreparedSource` now
  includes `grammar_proofs` in its fingerprint and rejects anything other than
  the exact source-ordered bootstrap route/proof bijection; non-bootstrap
  prepared sources require the empty proof tuple. The grammar proof now binds
  the closed authenticated-English evidence tuple and its language-evidence
  digest without the former circular route-digest dependency.
- Added focused positive and mutation proof at
  `memorii/tests/unit/core/semantic_ingestion/test_bootstrap_route_contracts.py`.
  Combined focused preparation evidence was `31 passed in 17.22s`; scoped
  Ruff, `py_compile`, and `git diff --check` passed.
- The admitted-observation projection/persistence and verified-profile
  deterministic producer remain unimplemented. The existing legacy observation
  cannot supply the required retained artifact, semantic context, projection,
  exact carrier sets/artifact, or authorization proof; using test helpers would
  fabricate those authorities.

## Historical Next Action (Superseded 2026-08-09)

Implement the canonical Step-1 admitted-observation record/projection with its
typed server-owned derivation inputs, then construct the verified-profile V1
producer only from that persisted authority and the embedded policy/corpus.

## Step-1 Admitted-Observation Persistence Progress (2026-08-09)

- Added a CTV-sealed retained Step-1 material payload to the canonical source
  record path. The record projection reloads strict retained-text, semantic
  context, projection, governance-carrier, message-admission, scope, and
  admission-authorization contracts and rejects payload or source substitution.
- Added the deterministic one-segment verbatim projection constructor. It
  creates retained/projection/local artifacts and exact mapping proof only from
  retained Unicode text plus authenticated server-owned context and carriers;
  no model or test helper participates in production construction.
- Focused evidence: the new Step-1 unit plus existing bootstrap-admission and
  atomic-store families completed with 35 passing nodes; scoped `py_compile`,
  Ruff, and `git diff --check` passed.
- This is not M3 closure: governed-conversation/delegation envelope ingestion
  and multi-segment construction remain unimplemented, and provider wiring and
  `PreparedSource` construction remain explicitly out of scope for this unit.

## Verified V1 Preparation Producer Progress (2026-08-09)

- Implemented `BootstrapTextPreparationProducer` in
  `memorii/memorii/core/semantic_ingestion/source_preparation.py`. It accepts
  only a `VerifiedBootstrapProfile`, uses only its embedded
  `TextPreparationPolicy` and grammar corpus, and refuses incomplete Step-1
  observation authority, substituted retained/context/carrier joins, missing
  authenticated language evidence, policy substitution, and nonmatching or
  unsupported corpus forms before publication. It contains no detector,
  generic language router, registry, provider, or test-only authority.
- The producer deterministically partitions each admitted parent projection at
  sentence boundaries (and only safe whitespace boundaries when a configured
  length limit requires it), builds exact child ownership/context spans,
  sentence/token references, `BootstrapDeclaredSegmentLanguageRoute`, and
  `BootstrapSegmentGrammarProof` tuples, then lets the existing source-owned
  repository perform the immutable idempotent/CAS publication. The new
  `TextPreparationService.for_verified_bootstrap_profile` factory is the
  canonical production construction surface; provider-root wiring remains
  explicitly out of scope.
- Added `memorii/tests/unit/core/semantic_ingestion/test_bootstrap_text_preparation_producer.py`.
  It proves a repeated equal-sentence, multi-child parent generates distinct
  child identities with exact proof/route/span joins and idempotent
  publication; it also proves source-text substitution, embedded-policy
  substitution, and corpus substitution fail closed without a prepared result.
- Local evidence: the multi-child vector completed `1 passed in 10.16s`; the
  three substitution/policy/corpus cases completed `3 passed in 26.88s`;
  existing semantic-pipeline coverage completed `15 passed in 9.40s`.
  `PYTHONPATH=memorii .venv/bin/python -m py_compile
  memorii/memorii/core/semantic_ingestion/source_preparation.py`, scoped Ruff,
  and `git diff --check` passed.
- Residual evidence: no provider composition, host current-release assertion,
  JSONL/failpoint, timing, or CI collection work was performed here. The
  producer shares the existing repository CAS boundary but this focused unit
  does not re-prove its durable atomic-store race matrix.

## Historical Next Action (Superseded 2026-08-09)

Continue the already-owned prepared-publication/handoff request construction
and its durable lost-ack/concurrency authority proof; do not add provider-root
wiring as part of this producer unit.

## Server-Owned Governance Material And Coordinator Adoption (2026-08-09)

- Added the canonical `derive_source_governance_material` builder in
  `memorii/memorii/core/memory_evolution/source_governance.py`. Given exact
  authenticated ingress, the provider event, and server receive/retain times,
  it deterministically derives the typed source context, semantic required
  scope projection, one segment binding, one message admission identity,
  carrier sets/artifact, and an admission scope authorization proof. Missing
  semantic egress/source authority, incomplete authorization coverage,
  unsupported scope projection, and interval-policy substitution return a
  typed `nonpromoting` result; no privileged field is defaulted from public
  event metadata.
- `ProviderIngestionCoordinator` now takes normal governed events through
  `ProviderEventNormalizer`, the builder, language-evidence binding via the
  admitted record constructor, and `prepare_atomic`; it no longer uses the
  legacy `_governed_source` copy for that route. Current metadata-poor hooks
  retain their evidence-only legacy branch and do not manufacture projection
  or governance material.
- The sealed CTV record continues to reload every exact governance contract.
  New focused proof covers deterministic happy-path construction/reload,
  missing host source authority, and source-interval policy substitution.
  Existing normal provider composition now reaches its allowed transport and
  terminal path through the admitted observation.
- Local evidence: `test_step_one_source_ingestion.py` completed `4 passed in
  6.34s`; `test_normal_provider_root_reaches_allowed_transport_once_and_terminalizes`
  completed `1 passed in 14.22s`; scoped `py_compile`, Ruff, and
  `git diff --check` passed.
- Residual work: governed structured snapshot/delegation envelope derivation,
  source-record durable restart/failpoint coverage, and full Step-2 handoff
  matrix remain outside this bounded normal verbatim-route correction.

## Provider Wiring Progress (2026-08-09)

- The ordinary `ProviderMemoryService` composition now derives the preparation
  service only from the verified bootstrap profile when a host runtime supplies
  its atomic store and writer admission owner. It installs the profile-bound
  `BootstrapTextPreparationProducer` through `TextPreparationService` and the
  store-owned `AtomicStorePreparedSourceRepository`; no caller-provided
  producer, router, remote capability, or fallback is accepted.
- Focused evidence: `PYTHONPATH=memorii .venv/bin/python -m py_compile
  memorii/memorii/core/provider/service.py`; `PYTHONPATH=memorii .venv/bin/python
  -m pytest memorii/tests/unit/core/semantic_ingestion/test_semantic_provider_composition.py::test_normal_provider_root_reaches_allowed_transport_once_and_terminalizes
  -p no:cacheprovider -q` (1 passed in 9.57 seconds); and `git diff --check`.
- Remaining blocker: the existing production coordinator still emits the
  legacy `_governed_source` record rather than the already-defined Step-1
  admitted-source record. Its reloaded `SourceObservation` consequently lacks
  the sealed semantic projection, context, governance carriers, and language
  evidence required by the profile producer. The producer correctly rejects
  that input before publication, so this composition-only change cannot yet
  establish prepared publication, bootstrap writer handoff, or a normal
  accepted semantic terminal. The next implementation step is to route the
  coordinator through the canonical Step-1 admitted-source builder using only
  authenticated ingress, provider event, and server-owned policy/timestamps,
  then bind its existing CAS/handoff owners.

## Empty Provider-Event Containment (2026-08-09)

- Confirmed cause: Hermes `sync_turn` legitimately emits one child event for
  each side of the turn, including an empty assistant string. The normalizer
  previously admitted that string, and the single-segment verbatim builder then
  attempted zero-length retained, projection, and local spans.
- `SourceAdmissionRequest.original_text` now rejects empty strings as a
  fail-closed contract defense. `ProviderIngestionCoordinator` recognizes an
  empty event before source identity, governance, atomic admission, prepared
  source publication, or writer handoff, returns the existing `source_only`
  evidence-only disposition, and creates no semantic source artifact.
- Focused proof: `test_provider_event_normalizer_rejects_empty_semantic_source_text`
  plus the three-case Hermes matrix (nonempty user/empty assistant, empty
  user/nonempty assistant, both empty) completed `3 passed in 73.94s` for the
  Hermes matrix. The matrix asserts only nonempty content has a retained
  semantic source record. The separate normalizer and normal-root tests passed
  in the combined run before the filesystem test began.
- The first filesystem restart/race rerun exposed a stale assertion that the
  fixture-owned `_CaptureTransport` observes one request (`0 == 1`). The
  fixture intentionally constructs `build_authorized_local_semantic_runtime`
  and keeps that capture transport unattached; the assertion now requires zero
  remote requests, while explicit-remote coverage remains separate. The
  corrected filesystem selector and normal accepted control then completed
  `2 passed in 201.24s`; the user event produced one semantic event batch and
  the accepted control retained its complete effect group.
- Static evidence passed: scoped `py_compile`, scoped Ruff, and
  `git diff --check`. No serialization optimization or persisted format change
  was made.

## Built-In Local Host Runtime Composition (2026-08-09)

- Added `BuiltInLocalHostSemanticIngestionCapability` as the canonical
  deterministic local host builder. It consumes only already host-verified V1
  material plus externally held deployment/current-release authority; it does
  not construct roots, release evidence, policy, or authorization bytes.
- A verified built-in profile now creates the paired
  `SemanticWriterAdmissionStore` and `SemanticIngestionAtomicStore`, installs
  the required `CurrentBootstrapReleaseVerifier`, and derives the profile-owned
  `BootstrapTextPreparationProducer` with its store-owned
  `AtomicStorePreparedSourceRepository` and `ProductionLocalSemanticAnalyzer`.
  Missing current-release authority produces no runtime, leaving the ordinary
  service evidence-only.
- `ProviderMemoryService`, its factory, `HermesMemoryProvider`, and the
  filesystem bundle accept the built-in capability directly. The filesystem
  composition creates its semantic-integrity lifecycle before built-in runtime
  construction so the atomic store and lifecycle share one linearization.
  Existing installed host capability discovery remains the compatibility path
  when no explicit built-in capability is provided.
- New no-entry-point-patch proof exercises provider, default Hermes, and
  filesystem construction from the same fixed host-verified V1 material and
  asserts paired writer/store, current verifier, profile-bound preparation, and
  local analyzer ownership. A separate proof removes the current-release
  verifier and confirms evidence-only fallback.
- Local evidence: `PYTHONPATH=memorii .venv/bin/python -m pytest
  memorii/tests/unit/core/semantic_ingestion/test_semantic_provider_composition.py::test_builtin_local_capability_wires_provider_hermes_and_filesystem_without_entrypoint_patch
  memorii/tests/unit/core/semantic_ingestion/test_semantic_provider_composition.py::test_builtin_local_capability_missing_current_release_verifier_is_evidence_only
  -p no:cacheprovider -q` completed `2 passed in 22.28s`. Scoped Ruff,
  `py_compile`, and `git diff --check` passed. The focused provider factory
  suite completed `16 passed in 4.27s`; the filesystem bundle construction
  check completed `1 passed in 4.42s`.

### Trust-Domain Constructor Closure (2026-08-09)

- Removed the public `trust_domain` constructor field from
  `BuiltInLocalHostSemanticIngestionCapability`. The capability now has no
  caller-controlled domain label: the provider composition root selects its
  required domain and accepts it only when both material and release-evidence
  domains from the host verifier result match that required domain.
- The runtime builder uses only that verifier result when deciding whether the
  scenario-only initial writer activation is legal. The scenario fixture
  remains a factory-owned, sealed scenario-material variant; production
  construction remains bound to production material. Neither factory can
  relabel a capability after construction.
- Focused construction proof asserts that the field is absent and that passing
  `trust_domain` is rejected before composition. Scoped `py_compile`, Ruff,
  and `git diff --check` passed. The old patched-entry-point accepted-control
  test remains an evidence gap: it supplies no host material verifier, so the
  current service correctly stays evidence-only rather than producing an
  accepted terminal. It must be replaced with a verified host composition
  fixture; this closure does not relax that boundary.

## Authenticated Structured Source Completion (2026-08-09)

- Added the narrow out-of-band `AuthenticatedStructuredSourceEnvelope` to
  authenticated ingress. It is bound to the exact public event ID and closed
  operation, has a content-addressed canonical JSON payload, and cannot be
  supplied or overridden by public `ProviderEvent` fields.
- `ProviderEventNormalizer` now requires that carrier for `session_end`,
  `pre_compress`, and `delegation_result`; it verifies byte equality with the
  public content, canonical JSON, the closed schema/version, exact envelope
  operation, and the governed message/context order. Missing or inconsistent
  envelope authority returns the existing evidence-only provider disposition.
- The server-owned projection builder retains the exact envelope bytes and
  creates one ordered segment for every authenticated snapshot message (or the
  one delegation result content), with source references, exact context/admission
  identities for messages, newline separator spans, and envelope-field mapping
  proofs. It never derives boundaries from text or public role/metadata.
- Focused evidence: `PYTHONPATH=memorii .venv/bin/python -m pytest
  memorii/tests/unit/core/semantic_ingestion/test_step_one_source_ingestion.py
  -p no:cacheprovider` completed `6 passed in 6.72s`. The proof covers repeated
  equal content, three-message boundaries/separator offsets, canonical pointer
  mapping, source-reference/context substitution, missing/out-of-band envelope
  denial, delegation result-only projection, and sealed record reopen equality.
  Scoped `py_compile`, Ruff, and `git diff --check` passed.
- The broader `test_bootstrap_source_admission.py` was invoked twice, but the
  local runner returned only partial progress and no terminal result; it is not
  claimed as passing evidence. No product failure trace was emitted.

## Transaction Group Plan Repository (2026-08-09)

- Added the coordinator-owned
  `AtomicStoreTransactionSemanticGroupPlanRepository` in
  `memorii/memorii/core/semantic_ingestion/transaction_group_plan_repository.py`.
  It creates typed repository references, has no standalone write method, and
  loads one complete canonical `TransactionSemanticGroupPlan` only from the
  fence-bound atomic-store generations for the admitted source. A foreign
  repository fingerprint, absent/ambiguous plan ID, undecodable typed plan, or
  plan digest mismatch fails closed. Legacy opaque terminal markers cannot
  satisfy a typed plan reference.
- `SemanticTerminalPersistenceService.persist` now accepts an optional real
  transaction group plan and includes it as the unique `plan` member of the
  planned checkpoint. The atomic-store validator independently accepts only a
  canonical typed plan whose source binds the checkpoint fence; it retains the
  pre-lineage marker format only when no plan has been supplied. This is an
  atomic publication boundary, not a plan-only `put()` path.
- Focused local evidence: `PYTHONPATH=. ../.venv/bin/ruff check
  memorii/core/memory_evolution/atomic_store.py
  memorii/core/semantic_ingestion/transaction_group_plan_repository.py
  memorii/core/semantic_ingestion/persistence.py
  memorii/core/semantic_ingestion/__init__.py
  tests/unit/core/semantic_ingestion/test_transaction_group_plan_repository.py`
  passed. `PYTHONPATH=. ../.venv/bin/python -m pytest
  tests/unit/core/semantic_ingestion/test_transaction_group_plan_repository.py
  tests/unit/core/semantic_ingestion/test_source_group_plan_contracts.py
  -p no:cacheprovider` passed `9` tests in `16.90s`, including an actual
  planned-checkpoint publication followed by a typed atomic-store reload.
- Exact residual: the legacy terminal pipeline has no sealed graph snapshot,
  graph expansion, planning artifact, or planning authorization from which a
  valid `TransactionSemanticGroupPlan` could be derived. It must continue to
  pass no plan rather than inventing one. The later graph-dependent coordinator
  must construct those approved inputs, call this atomic checkpoint path with
  its completed plan, and then reload the referenced plan before creating each
  group authorization and terminal persistence request.

## Historical V1 Authority Artifact And Scenario Boundary (Superseded 2026-08-09)

- Regenerated `ctv-binding-authority-v2.json` from the canonical design and
  registry using the clean-room compiler. The current checked identities are
  design `feabcb0dad3f72cebf2c3b86899f48c570f4db29a48d3821462f7fd57ed99cc9`,
  registry `8c5ad6e6260c793472ddbc2df8637230fbb5d5b28405b0b558ac4491c945d37e`,
  authority `4c25196243d5a825982a00c20b2650447d3b73ca6d31c42db81e7148a66d51e9`,
  validator `317133f2e92ad8032968314b3f16ff1b08b031c93c0ebcd3bbd789a876de5d6d`,
  and checker `e2c35870a99e587f34cbffc701f42587520ee015009cd51647367da56716c732`.
  Workflow, documented command, and parity/gate test pins were updated in the
  same change. The exact isolated checker passed its two-replica self-test.
- The originally planned scenario regeneration cannot truthfully proceed.
  `run_scenario_ingress.py` still assumes the retired direct
  `EnglishRuleMemoryExtractor` callback and crashes because normal provider
  construction is correctly evidence-only without verified host capability.
  More importantly, the governing scenario authority requires the four-case
  `owner`, `status`, abstention, and ambiguous-owner meanings, whereas the
  approved V1 bootstrap corpus currently admits only the exact supported text
  `Atlas owner is Bob.`. The scenario's rendered `Atlas owner is Alice.`,
  `Orion status is running.`, no-assertion, and two-owner inputs are all outside
  that corpus. Wiring the runner to `BuiltInLocalHostSemanticIngestionCapability`
  would therefore produce unsupported/abstained terminals, not the authority's
  required match/ambiguous outputs.
- No scenario manifest, fixture, structural spool, bootstrap anchor, or signed
  host evidence was fabricated or replaced. The linked design delta
  `docs/work/semantic_ingestion/scenario-v1-runtime-closure-2026-08-09/design.plan.md`
  selects the smallest complete V1 path: four literal corpus forms, the exact
  two-segment owner ambiguity, a public-vocabulary local analyzer, and a
  scenario-test material injection into the ordinary built-in host capability.
  It preserves the historical scenario bytes and does not create a fixture-only
  semantic path or activate scenario trust in production.

## Historical Next Action (Superseded 2026-08-09)

- Run the independent delta review for
  `scenario-v1-runtime-closure-2026-08-09`. On approval, implement its exact
  corpus/preparation/analyzer/runner slice, then regenerate all V1 and
  scenario/CTV authority from one frozen candidate. Do not run the retired
  direct-extractor scenario runner or fabricate signed authority.

## Scenario Runtime Implementation Boundary (2026-08-09)

- The scenario V1 runtime slice now uses the ordinary built-in host capability
  and `ProviderMemoryService`; it no longer invokes the retired direct
  extractor. Scenario material remains explicitly bounded to `scenario_test`.
- A deterministic writer-mode failure was isolated in the linked debugging
  WorkPlan `docs/work/semantic_ingestion/scenario-writer-activation-regression-2026-08-09/debug.plan.md`.
  The root cause was a missing host-owned certified initial writer activation,
  not a terminal persistence defect. The correction preserves the default
  evidence-only writer and grants the sealed scenario fixture its certified
  empty migration only.
- Focused proof is green: the scenario host activates verified semantic mode;
  the no-activator sibling remains evidence-only; the temporary four-case
  public-ingress run has five observations with `match`, `match`, `match`,
  `abstain`, and `match`; independent A/B elaborations and spool parity passed.
- No signed authority, CTV, checked-in manifest, or spool has been regenerated
  from this working tree. That remains a coordinator-owned frozen-candidate
  step after independent debug review.

## Multi-Segment Pipeline Correction (2026-08-09)

- `SemanticIngestionPipeline` now accepts a complete nonempty prepared source
  with multiple child segments. It resolves the exact child route from each
  analysis segment identifier rather than using the first route.
- Each consensus route digest and copied predicate/argument span must bind the
  selected child route. The closed owner ambiguity additionally requires two
  distinct child-route coordinates from one source; swapped, duplicate,
  equal-value, and separate-source forms cannot produce the protected result.
- Focused direct proof passed: `test_multi_segment_route_selection_rejects_swapped_duplicate_and_wrong_source_evidence`
  (`1 passed in 5.02s`), with scoped Ruff, `py_compile`, and diff hygiene
  passing. The real public-ingress ambiguity runner was started but this local
  harness did not return a terminal pytest result; do not record it as a
  suite-pass claim.

## Bootstrap Authority Linearization Remediation (2026-08-09)

- Corrected the final-review authorization gap in the atomic bootstrap owner.
  Both prepared-source publication and writer handoff now run a fresh,
  host-owned current-release check as the memory-plane transaction
  precondition, immediately before records become visible. Malformed,
  wrong-type, unavailable, or OS-failed verifier responses fail closed before
  any part of the batch writes.
- The supplied ephemeral assertion is now strictly revalidated against the
  release evidence. It remains non-persisted; it cannot stand in for the
  commit-bound host check.
- Both retained-pending and prepared-published authority-unavailable terminals
  are now permanent source outcomes. Retries and reauthorization observe the
  terminal before any work, while publication and handoff include the terminal
  as a write-CAS precondition; no later bootstrap publication can reopen the
  source.
- Focused local evidence: `PYTHONPATH=memorii .venv/bin/python -m pytest
  memorii/tests/unit/core/semantic_ingestion/test_semantic_atomic_store.py -q
  -p no:cacheprovider` passed `22` tests in `5.44s`; scoped Ruff passed.
  The normal provider-root smoke selector currently returns
  `ingress_unavailable` before this authority path; it is a separate current
  provider composition regression and not claimed as proof for this change.

## Scenario Child-Corpus Admission Correction (2026-08-09)

- Removed the synthetic whole-source owner-ambiguity literal from the
  scenario-only bootstrap corpus. The scenario profile now authorizes only
  individual source literals; it no longer treats a concatenated ambiguous
  source as a distinct grammar form.
- Provider ingress now builds the sealed Step-1 projection before making the
  bootstrap admission decision. For one child it preserves the existing exact
  classifier. For multiple deterministic sentence children, every normalized
  child must independently select an exact corpus literal; otherwise the
  source is retained as unsupported and never reaches writer handoff. Step-2
  repeats this same partition and records the exact route/proof for each child.
- Focused static evidence passed: `py_compile` for provider ingestion and
  source preparation; Ruff on the changed provider, producer, scenario fixture,
  and test files; `git diff --check`; and collection of the four new provider
  ingress cases. Direct residue and boundary mutation calls passed, each
  producing `unsupported_grammar` with no candidate. A direct eligibility
  check for the two-owner source returned
  `selected_pipeline_pending` without a whole-source case. The full accepted
  and ambiguity provider calls were started but this local harness returned no
  terminal result; they are explicitly not claimed as passing evidence.

## Graph-Dependent Coordinator Scope Decision (2026-08-09)

The final coordinator implementation audit verified that the remaining approved
contract is not a small integration omission. The legacy accepted provider path
produces a `SemanticTerminalOutcome`, but it has no production owner for a
sealed `SourceProposalAlignment`; a `GraphSemanticSnapshotBundle` and typed
read-set extensions; graph-dependent normalization, reconciliation, reference
closure, and transaction-group expansion; a loadable planning-artifact
repository and pure compiler; or the graph CAS/retry owner. Repository search
finds only the typed contracts and the atomic transaction-plan read view; it
finds no producer or canonical owner for those required graph artifacts.

The governing design makes the missing sequence explicit:

- `docs/design/semantic_ingestion_architecture.md` lines 15673--15712 require
  graph-free source grouping before snapshot-bound transaction expansion.
- Lines 16940--17210 require full typed compilation requests, independently
  loadable planning artifacts, exact fixed-point plans, and reload-derived
  per-group authorization.
- Lines 17680--17725 assign the coordinator its snapshot, extension,
  expansion, compiler, graph-CAS/retry, and attempt-before-use sequence.
- Lines 20535--20590 require restart/lost-ack and multi-group replan proof that
  preserves the committed group's original authorization while replacing only
  the retried group.

Creating these values from the legacy terminal pipeline would recreate the
fabricated authority that this WorkPlan deliberately removed. No production
code was changed by this audit.

The two mutually exclusive scope alternatives requiring a user decision are:

1. Expand M3 into separately bounded Step 5--8 implementation milestones for
   the missing source-alignment producer, snapshot/read-set repository
   interfaces, graph expansion/reconciliation/closure, planning-artifact
   repository and pure compiler, graph CAS/retry owner, and append-only
   attempt/lineage/result persistence with focused accepted, unresolved,
   replan, restart, and lost-ack proof.
2. Move graph-dependent coordinator acceptance to a later milestone through a
   linked design review that revises M3's completion contract. M3 retains the
   current non-fabricating terminal behavior and cannot claim that accepted
   terminals are graph-plan-authorized.

## Current Exact Next Action

Build one complete non-test trusted-host `SourceNormalizationAuthorityBundle`
with sealed producer resources, then prove real owner execution and JSONL
restart continuation through direct, factory, filesystem, and Hermes roots.
The shared coordinator now passes the actual bootstrap handoff to the injected
provider and owner, rejects every absent/noncommit/malformed/stale/substituted
normalization return before terminal work, and forwards the exact typed reload
and publication reference to `pipeline.run` for repeat validation. The binding
artifact records that no root currently constructs the complete host bundle and
real execution owner, so end-to-end non-test caller count remains zero.
The linked terminal-persistence testing WorkPlan remains the sole owner of
serialization profiling, timing recapture, shard topology, and PR-gate
performance evidence.

- 2026-08-10 V3 public-root execution blocker: production
  `BootstrapTextPreparationProducer` correctly emits a
  `BootstrapDeclaredSegmentLanguageRoute`, which deliberately has no generic
  `resource_binding`.  The current V3 predicate and temporal lane payload
  contracts nevertheless embed generic `PredicateEventInventory` and
  `TemporalResolution` values.  Their existing validators require every
  selected route to expose `resource_binding`, so neither payload can be
  instantiated for the real bootstrap route even when the distinct
  `BootstrapAnalysisRouteBindingSet` supplies the exact predicate/temporal
  manifests.  This makes a configured `ProviderMemoryService.sync_event`
  normal-root V3 run impossible without an impermissible generic-route bridge.

  The approved remedy must add complete V3-native predicate-event
  candidate/inventory and temporal candidate/resolution contracts (including
  their source, segment, manifest, provenance, ordering, codec, and
  interpreter joins), then change only the bootstrap V3 adapters and evidence
  producer to emit those values.  Generic lane results remain the non-bootstrap
  path; bootstrap routes must not gain `resource_binding`, and V3 must not wrap
  generic results.  Pending that bounded design amendment, the public-root
  normal success, memory/JSONL lost-ack reopen, and missing/mismatched
  authority tests remain blocked.  No production or fixture workaround was
  added.

- 2026-08-10 persisted-schema repair: `SourceNormalizationRequest`, its
  evidence manifest, result capability registry, and atomic execution policy
  now retain their exact strict authority types rather than bare `BaseModel`.
  The normal host vector proves stage-built request `model_dump`/`model_validate`,
  canonical encode/decode byte equality, atomic publication, and reloaded
  result equality. Recovery now joins the public `operation_id` to the fence's
  operation ID and checks the marker's distinct fenced pending ID plus exact
  `DeliveryIdentity` and `SemanticWriterCommitBinding`. This is partial M3
  evidence only: the next action remains one real-authority success through a
  public root and memory/JSONL lost-ack reopen proof.

- 2026-08-10 bootstrap projection foundation: the canonical derivation
  authority now requires a strict `BootstrapAnalysisRouteBindingSet` and the
  execution owner verifies its exact declared-bootstrap-route bijection before
  any producer work.  The new ephemeral `BootstrapAnalysisRouteProjection`
  and scalar `BootstrapAnalysisProvenanceV1.from_binding` seal the declared
  route, host-certified resource binding, and four lane manifests without
  adding any authority to `PreparedSource`.  Focused contract proof covers the
  exact join and swapped binding rejection (`5 passed`); scoped Ruff, Python
  compilation, and diff-check pass.  This is only the schema/authority
  foundation: proposal, lane, persisted V3 codec, and public-root migration
  remain pending, and the exact next action below is unchanged.

- 2026-08-10 bootstrap V3 request boundary: added distinct closed V3 codecs
  for bootstrap segment-analysis input, proposal request, and Stanza/spaCy,
  predicate-event, and temporal lane requests.  These carry the required
  flattened provenance and have V3-only digest domains; no optional field was
  added to a V2 preimage.  The semantic-contract registry recognizes each V3
  type.  The current adapters and sealed producers still consume V2 generic
  requests, so this is a codec/contract migration prerequisite rather than an
  owner-vector completion claim.  Focused proof is now `6 passed`.

- 2026-08-10 bootstrap V3 aggregate boundary: added separate flattened V3
  proposal-run, lane-result, normalization-request, evidence-manifest, and
  normalization-result codecs.  The request validator requires exactly four
  lane receipts per provenance segment and the result closes over the same
  provenance tuple.  These types are registered independently from V2.  They
  are not yet produced by the sealed adapters or published by the stage, so
  no bootstrap normal-vector, atomic V3, or reload claim is made.

- 2026-08-10 bootstrap V3 producer/stage feasibility blocker: the added V3
  aggregate contracts retain only the proposal payload digests and lane payload
  digests.  They do not retain a closed typed proposal payload, typed Stanza or
  spaCy analysis, typed predicate-event inventory, or typed temporal result;
  nor do they define an authenticated read-back/projection owner for those
  values.  `GraphFreeSourceNormalizationStage` must consume exactly those
  values to construct interpretation, alignment, the existing normalized
  result, and the atomic member closure.  Consequently it cannot construct a
  V3 request/result from the V3 aggregate alone.  Reconstructing a V2
  `SemanticProposalRun` or generic lane requests from the digest-only V3
  records would be the expressly forbidden V2 bootstrap bridge and would not
  authenticate the retrieved bytes.

  The affected V3 schemas are `BootstrapSemanticProposalRunV3`,
  `BootstrapAnalysisLaneResultV3`, `BootstrapSourceNormalizationRequestV3`,
  `BootstrapSourceNormalizationEvidenceManifestV3`, and
  `BootstrapSourceNormalizationResultV3`; the affected canonical consumers
  are `SealedSemanticProposalRunProducer`,
  `SealedSourceNormalizationEvidenceProducer`, the four local adapters, and
  `GraphFreeSourceNormalizationStage`.  Two coherent alternatives require a
  governing design decision: (A) add direct, strict, content-addressed typed
  payload carriers to the V3 proposal and four lane result contracts, with
  their exact proposal/analysis/event/temporal joins; or (B) specify a
  separately authenticated, capability-bound V3 read-back/projection
  repository that returns those typed values and proves their digests before
  the stage consumes them.  Neither alternative may add optional V2 fields or
  a V2 fallback to the bootstrap path.

  No production code, fixture, or test was changed for this infeasible slice.
  Next action: obtain and freeze the design decision selecting (A) or (B),
  then resume the sole writer for the corresponding complete producer, adapter,
  stage, atomic-publication, and normal-vector migration.

- 2026-08-10 Option-A implementation clarification: the approved payload
  carrier wording requires a full V3 proposal subtree before the bootstrap
  producer can be migrated.  The existing `SemanticProposal`,
  `SemanticProposalAttemptIdentity`, and transport normalizer each contain a
  required generic `SegmentLanguageRoute`; therefore they cannot be placed in
  a bootstrap carrier without violating the explicit V3 prohibition on generic
  routes and the no-bridge rule.  The architecture names
  `BootstrapProposalAttemptV3` and `BootstrapNormalizedProposalV3` but does
  not yet give their exact fields, codecs, nested proposal-member closure, or
  a V3 normalizer boundary.  The only safe next action is the bounded design
  correction that freezes that subtree and its direct transport/normalization
  contract; no producer, adapter, stage, fixture, or atomic migration may use
  wrappers or generic routes before it exists.

- 2026-08-10 V3 operation-algebra completion blocker: the later corrected
  proposal subtree removes generic routes but `BootstrapNormalizedProposalV3`
  still lacks the typed semantic operation-member algebra consumed by
  `PreAlignmentSemanticOperationSubjectSet` and source alignment (predicate
  identifiers; fact/correction/retraction/action/identity member coordinates;
  typed entity/literal values; and operation digests).  Its displayed
  subject/predicate/object spans and quotes cannot deterministically produce a
  semantic operation, so the existing interpretation consumer would have to
  rebuild a forbidden V2 `SemanticProposal`.  A V3 operation-member algebra
  and V3 interpretation consumer are required before runtime wiring.

## Source-Normalization Recovery Persistence Prerequisite (2026-08-10)

- Added the concrete sealed source-normalization evidence producer. It accepts
  only host-injected Stanza, spaCy, Duckling, predicate-detector, and typed
  interpretation/identity lane dependencies; derives exact `SegmentAnalysisInput`
  values from `PreparedSource`; validates the complete route-resource tuple and
  consumed reservation; and assembles the V2 analysis, event, temporal,
  interpretation, consensus-selection, evidence-entry, and graph-free stage
  inputs. Missing/swapped/ambiguous lanes fail as the closed
  `evidence_sealed/analysis_unavailable` noncommit. This is partial M3 work:
  provider composition has not yet constructed the required reservation and
  trusted dual-clock runtime, so there is still no production caller.

- Added the non-reachable closed execution-owner contract in
  `source_normalization_execution.py`: it builds per-call recovery bindings,
  probes recovery before time/reservation/producer work, enters reservation
  only on `absent`, validates phase-specific noncommits, and releases a
  reservation in `finally`. The module exports strict authority, reservation,
  trusted-time, and sealed-producer protocol surfaces. This is partial M3
  evidence only: no concrete producer or non-test production composition call
  exists yet.

- Added strict, closed recovery invocation/handoff/authority/context/request
  and found/absent/publication-unavailable contracts. Each recomputes its CTV
  digests and validates source, preparation, operation, fence, authority, and
  expected-generation joins before persistence access.
- Added the canonical source-normalization repository recovery read boundary
  and atomic-store recovery-index read/snapshot API. A found result decodes
  only the indexed, sealed source-normalization generation; it never rebuilds
  a result. A validated context substitution returns the bounded
  `publication_unavailable/context_mismatch` outcome before the store read.
- Focused evidence: repository tests pass (`5 passed`); scoped Ruff and
  `py_compile` pass; `git diff --check` passes. JSONL/reopen/lost-ack,
  committed-index publication, and production reachability remain owned by the
  next execution-owner slice because no execution owner or composition root
  was added here.

- Coordinator handoff remediation: `SourceNormalizationExecutionOwner` now
  implements `SourceNormalizationExecutionOwnerProtocol` and returns only a
  revalidated `SourceNormalizationResult` or its closed noncommit.
  `ProviderIngestionCoordinator` repeats the exact source,
  preparation, fence, and publication-coordinate validation before calling
  `pipeline.run`, which repeats it before any pipeline/terminal stage. Missing,
  arbitrary, invalid, or noncommit returns produce only
  `source_alignment_authority_unavailable` with no terminal control. This is
  partial M3 evidence: the four public roots still have zero end-to-end
  production callers because no host constructs the full authority bundle.

## Linked Debugging Slice (2026-08-11)

- The provider-root bootstrap handoff CTV failure is tracked in
  `docs/work/semantic_ingestion/provider-root-delivery-identity-ctv-debug-2026-08-11/debug.plan.md`.
  That slice confirmed the V3 marker constructor skipped the existing
  model-lowering step before hashing and now applies the same lowered Python
  dump used by the V1 marker.
- The follow-on writer-governance closure registered the two recovery
  namespaces and admits only the exact V3 handoff shape. The direct
  no-authority provider smoke now passes at its expected fail-closed outcome;
  the linked debug packet records the focused evidence.

## Native V3 Functional-Vertical Status (2026-08-11)

- The ordinary accepted-fact path now has one production owner chain:
  `ProviderMemoryService.sync_event` ->
  `ProviderIngestionCoordinator._run_semantic_ingestion` -> retained V3
  recovery probe/claim -> `SourceNormalizationExecutionOwner` -> atomic V3
  normalization reload -> `BootstrapGraphHostBundle.execute` ->
  `BootstrapGraphDependentCoordinatorV3`. The coordinator uses the native
  compiler, per-group authorizer, group CAS repository, terminal preparation,
  and terminal reload owners; it does not construct graph authority from the
  transcript.
- Direct, factory, filesystem, and Hermes constructors all carry the same
  explicit source-normalization and graph host builders. Omitted host graph
  authority returns `graph_transaction_authority_unavailable` before graph
  effects. Omitted source-normalization authority returns
  `source_alignment_authority_unavailable` before analyzer work.
- Locally reproduced on this revision: the ordinary no-authority public root
  fails closed (`1 passed in 15.30s`), and scoped compile/Ruff validation of
  the coordinator, repository, host, source-normalization, capability, and
  all four composition roots passes. The existing normal-fact and JSONL
  recovery selectors require more than the interactive command window; their
  revision-bound execution remains the next evidence step, not a new design
  or implementation blocker.
