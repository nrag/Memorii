# Current Temporal Projection Interval Regression

- Work ID: current_temporal_projection_interval_regression
- Work type: debugging
- Status: complete
- Coordinator: Codex main thread
- Created: 2026-08-04
- Last updated: 2026-08-04
- Parent WorkPlan: `docs/work/semantic_ingestion/implementation.plan.md`
- Related WorkPlans: `docs/work/semantic_ingestion/testing.plan.md`; `docs/work/semantic_ingestion/debug-m4-closure-regressions.plan.md`
- Canonical inputs: `docs/design/semantic_ingestion_architecture.md`; `docs/design/semantic_temporal_retrieval.md`; current projection history/policy migration implementation
- Expected outputs: expected-vs-observed classification, interval/view invariant correction or test migration, focused replay/reopen proof, and independent closure review

## Objective

Restore the current temporal projection contract after reopen by determining
whether an atemporal (`valid_interval=None`) record is valid in that view or a
misrouted projection, then applying the narrow governing correction.

## Completion Contract

Complete only when the exact node passes, temporal/atemporal view semantics are
traced to governing design, current/historical/contested/reopen siblings pass,
unsupported projection shapes fail closed, and targeted independent review has
no required finding.

## Incident Or Symptom

The fresh uncontended M4 timing run stopped after 84 passes. In
`test_real_terminal_updates_publish_immutable_projection_generations_after_reopen`,
`current_temporal(...).projections` contained a `TemporalProjectionRecord` with
`valid_interval=None`; the expected current temporal view sorts interval starts.
The timing artifact was discarded.

## Hypothesis Ledger

| ID | Hypothesis | Experiment | Status |
| -- | ---------- | ---------- | ------ |
| H1 | identity or atemporal graph records are incorrectly included in temporal projection generation | inspect producing batch/carrier and projection derivation | rejected: the `None` projection is a typed claim-slot projection over two finite claim carriers |
| H2 | `None` is a supported atemporal partition and the test incorrectly assumes every current projection is interval-bearing | trace design view contract and existing atemporal tests | rejected for this incident: genuine atemporal projections are supported, but neither source claim is atemporal |
| H3 | commit-time materialization lost a previously required interval during persistence/reopen | compare pre-persist terminal, stored event, genesis replay, and reopened projection bytes | rejected: persistence and reopen preserve the already-invalid generated bytes |
| H4 | same-policy incremental reprojection replaces an already-built valid-time atom with a second resolver result over the carriers' original whole intervals | instrument the exact second commit and trace `_valid_time_partitions` through `_typed_claim_projection_records` | confirmed: the overlap atom is replaced by a contested resolver closure whose `resolved_interval` is `None` |

## Delegation And Cost Ledger

| Phase | Task | Role/tier | Ownership | Status |
| ----- | ---- | --------- | --------- | ------ |
| reproduce/map | isolate producing record and view contract | Spark explorer/code-mapper | read-only | complete |
| fix | apply invariant-level correction/test migration | worker, Terra-class | sole writer | active |
| review | targeted spec/correctness/test closure | Terra-class reviewers | read-only | pending candidate |

## Next Action

Return to `docs/work/semantic_ingestion/testing.plan.md` and restart its full
M4 broad-owned timing capture from scratch.

## Blockers And Limits

Budget used: one confirmed causal experiment, one fix batch, and one targeted
test-proof remediation. No external blocker remains.

## Outcome And Retrospective

The exact reproduction showed two finite claim carriers, `[2026-01-01,
2026-01-16)` and `[2026-01-11, 2026-01-21)`. `_valid_time_partitions` correctly
created three finite atoms. `_typed_claim_projection_records` then reran the
temporal resolver against the original whole-interval evidence and overwrote
the overlap atom with the resolver's contested `resolved_interval=None`.
Persistence, checkpoint replay, and reopen retained those generated bytes
exactly. Genuine atemporal projections remain valid and must not be filtered
from `TemporalProjectionView`.

## Progress Log

- 2026-08-04: Applied the invariant-level correction in
  `_typed_claim_projection_records`: a non-null partition atom remains the
  projection coordinate while a null atemporal/reference-only partition may
  still adopt the resolver result. Extended the exact same-policy persistence
  regression with finite partial overlap plus a separate genuine-atemporal
  claim under the same optional policy fingerprint.
- 2026-08-04: The first post-fix exact run reached the strengthened historical
  assertion, then failed after 61.86 seconds because the test had not imported
  `TimeInterval`. This is a test-authoring error rather than a product signal;
  the import is corrected before rerunning the same node.
- 2026-08-04: The corrected exact regression passed in 205.13 seconds. It proves
  three exact finite partial-overlap atoms, a separate genuine-atemporal atom,
  first-generation historical visibility, checkpoint/genesis equivalence,
  checkpoint-tail equivalence, and byte-identical reopened current projections
  under one same-policy incremental path.
- 2026-08-04: The valid-time atom and atemporal replay matrix passed 8/8 in
  8.11 seconds. The retained-evidence atemporal migration and reference-only
  promotion siblings passed 2/2 in 119.07 seconds, confirming the correction
  does not prevent legitimate `None -> finite` temporal-policy rebasing.
- 2026-08-04: The complete event-replay and projection-history sibling files
  passed 67/67 in 78.01 seconds. Repository Pyright reported zero errors, Ruff
  passed the two changed files, and `git diff --check` was clean before the
  final review-requested historical assertion.

## Review Log

- Spec audit: no finding. The correction conforms to the finite half-open atom,
  separate atemporal partition, and resolver contracts while preserving
  reference-only `None -> finite` migration.
- Test review finding 1: confirmed (`P2`, `changes_required`, verification).
  The regression proved the first historical generation but not the second,
  overlap-bearing generation. Added an exact `system_as_of=NOW + 1 hour`
  equality assertion for all three finite atoms.
- Test review finding 2: duplicate/unsupported as required. An overlap-specific
  lost-ack scenario would repeat transaction-stage coverage from
  `test_retry_and_lost_ack_are_byte_idempotent` and projection-generation
  reopen deduplication from
  `test_filesystem_reopen_recovers_exact_terminal_without_duplicate_effects`;
  the causal interval regression already proves exact checkpoint/genesis and
  reopened projection bytes.
- Correctness review: no actionable finding. The finite-coordinate guard,
  same-policy regression, atemporal/reference promotion, and bounded migration
  siblings are coherent; broader migration/race execution remains outside this
  debug slice.
- Test delta review confirmed the overlap-bearing historical assertion resolves
  finding 1. Its repeated lost-ack request remains classified duplicate and
  unsupported as a required addition for this causal boundary.

## Evidence Log

- Before fix: exact timing run failed the overlap-bearing same-policy current
  view because its contested projection had `valid_interval=None`.
- Final exact regression: `1 passed in 220.04s` with `-W error`, no cache,
  covering finite partial overlap, overlap-bearing historical generation,
  first historical generation, genuine atemporal separation, immutable
  generations, checkpoint/genesis equivalence, checkpoint tail, and JSONL
  reopen equality.
- Replay atom siblings: `8 passed in 8.11s` for disjoint, touching, partial
  overlap, nested, equal, open-ended, late arrival, and atemporal partitions.
- Retained temporal-policy siblings: `2 passed in 119.07s`, including genuine
  atemporal migration and reference-only `None -> finite` promotion.
- Surrounding deterministic families: event replay plus projection history
  passed `67 passed in 78.01s`.
- Static checks on the final tree: repository Pyright `0 errors`; Ruff `All
  checks passed`; `git diff --check` exited zero.
- A broader policy-migration-file run was deliberately interrupted when the
  coordinator bounded verification to the planned sibling matrix; it had 10
  passes and no failure and is not claimed as completion evidence.

## Revision-Bound Closure Record

```yaml
base_revision: 2a7a55e2f1ea265a5c7f824db1a38ce07cd9fb93
reviewed_revision: dirty-tree-target-db9abe612bd968f95e0111083ecf2922b4a4e3a453b754dfa32070747d19f3d6
tested_revision: dirty-tree-target-db9abe612bd968f95e0111083ecf2922b4a4e3a453b754dfa32070747d19f3d6
tested_tree_digest: db9abe612bd968f95e0111083ecf2922b4a4e3a453b754dfa32070747d19f3d6
tree_state: dirty M4 implementation tree; target production/test hashes recorded
changed_surface_inventory_complete: true for this debug delta
scope_delta_resolved: true
authority_chains_complete: true
required_local_jobs:
  - exact same-policy finite/atemporal persistence regression
  - replay atom matrix
  - retained-evidence temporal-policy siblings
  - event-replay and projection-history sibling files
  - Ruff
  - Pyright
  - git diff --check
passed_local_jobs:
  - exact same-policy finite/atemporal persistence regression
  - replay atom matrix
  - retained-evidence temporal-policy siblings
  - event-replay and projection-history sibling files
  - Ruff
  - Pyright
  - git diff --check
known_local_failures: []
failure_exclusions: []
workflow_identities: []
ci_event: not_applicable; local debugging operation
ci_executed_sha: not_applicable; no CI run
ci_executed_ref: not_applicable; no CI run
remaining_validated_p1_p2: []
remaining_blocks_approval: []
remaining_changes_required: []
local_ci_parity: diagnostic dirty-tree evidence; final CI belongs to parent implementation/testing closure
acceptance_gate_inventory:
  - same-policy temporal projection derivation
  - historical/current/checkpoint/reopen projection views
  - genuine atemporal partition separation
  - retained-evidence policy migration non-regression
github_run_urls: []
pr_head_sha: not_applicable; no PR
pr_base_sha: not_applicable; no PR
merge_base_sha: 2a7a55e2f1ea265a5c7f824db1a38ce07cd9fb93
required_checks_green: not_applicable; no CI run
```
