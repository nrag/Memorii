# Native Terminal Publication Debugging

- Work type: debugging
- Status: complete (bounded source-finalization debugging only)
- Parent: ../engineering-closure/milestones/05-authenticated-observer-comparator.plan.md
- Coordinator: root; all source writers stopped after candidate freeze
- Baseline HEAD: 191826cd3afb38bf605a337a71d576063b3bae5e, authorized dirty integration
- Expected: real provider sync persists one typed native source-finalization delta
  with its terminal result and returns source_only under the deterministic fixture.
- Observed: direct/factory/filesystem return graph_transaction_authority_unavailable;
  fixture reports BootstrapGraphDurableRetryProgressV3. Single direct reproduces
  before and after the first digest-shape fix (65.01s and 61.41s respectively).
- Scope: construction regression in new source-finalization integration. No
  classification as pre-existing. External keys/policy values are irrelevant.

## Hypotheses

1. Terminal request/preparation digest or type construction still fails. Supported
   by the confirmed initial request digest omission; corrected but symptom persists.
   Wrapper at preparation will identify exact exception or establish successful return.
2. Terminal store/manifest/writer validation rejects the newly added member or
   observation coordinate. Store wrapper will distinguish from preparation failure.
3. The fixture/coordinator classifies a valid terminal as durable retry. Successful
   preparation/store returns with retry output would support classification failure.

## Experiment And Evidence

Run one direct public selector with diagnostic wrappers that preserve original
return values and re-raise original exceptions. Print exact stage and traceback;
no production validation or failure behavior is relaxed. Keep code stable during
capture. Existing /tmp logs are diagnostics only; retain selected evidence here
when its cause is established.

## Completion Contract

Identify a causal mechanism explaining trigger, exception, conversion to durable
retry and provider result; implement the narrow invariant correction; prove the
same reproducer plus relevant sibling terminal/replay/compatibility cases; obtain
independent spec/correctness/test review. Parent observer and M5 remain incomplete.

## Captured Cause

The exact wrapped reproducer failed in 54.63 seconds. `capture.log` shows that
terminal preparation returns successfully, then terminal member serialization
calls `encode_semantic_contract(SourceFinalizationObservationDelta)`. That type
is not a registered semantic-contract envelope, and raises
`SemanticContractCodecError`. The coordinator's existing ValueError recovery
branch converts the exception into durable retry; the provider reports unavailable
graph transaction authority. `capture-inputs.json` pins the eight affected files.

H1 is weakened for the current candidate: preparation and its digest validation
succeed. H2 is confirmed at member codec selection, before CAS. H3 is disproved:
retry is caused by a real pre-CAS serialization exception, not misclassification
of a successful terminal. The earlier request digest omission was a separate
confirmed construction defect; its correction did not repair this codec path.

The new source member's decoder expects the native atomic-member envelope, so
merely adding a generic semantic-contract registration would mismatch the decoder.
The narrow fix selects its declared native encoder by member kind and enforces
the exact source-delta model; existing terminal members retain their existing
codec bytes. Focused native codec/identity substitution proof precedes rerunning
the public reproducer. Independent correctness challenge is active.

## Next Action

Resume the parent observer packet's group projection and persistence work.

## Correction Evidence

The same wrapped public reproducer now passes (1 passed, 68.79 seconds) in
`codec-fixed.log`. Both preparation and store return successfully; the source
delta is retained in the real terminal result. The codec change selects the
native encoder only for the new member, enforces its exact model, and preserves
all existing terminal member encodings. This is a causal before/after result,
not full observer or final debugging closure. Focused family tests and a genuine
historical terminal fixture from an isolated HEAD checkout are in progress.

The historical capture was generated by running the original direct public test
against an isolated archive of HEAD, then retaining request, intent, reload and
all persisted memory records under `tests/fixtures/semantic_ingestion/historical_terminal`.
The first current-code probe found legacy intent re-encoding was not byte-identical
and standalone reload decoding raised an unresolved Pydantic forward reference.
These are confirmed compatibility defects in the authorized integration, not
pre-existing failures. The sole writer is correcting canonical field lowering
for the three explicit legacy shapes and codec-triggered model initialization.
Exact historical byte and persisted replay proofs remain required.

After the compatibility correction, standalone decoding and re-encoding of all
three genuine old contracts preserves exact bytes. The failed-first-group
filesystem scenario passes (1 test, 91.52 seconds), proving source finalization
with zero completed groups. The next run passed the three compatibility cases
and existing missing-group proof, then failed the missing-source test solely
because it expected a later diagnostic; the canonical member-closure check
already rejects the missing record. That expectation is corrected. A separate
historical restore test initially attempted an unauthorized governed write;
its setup now uses the existing JSONL backend batch rehydration path. No product
authorization was relaxed. Historical captured record contents are unchanged;
the reconstructed batch is a test transport, not original transaction history.

The coordinator is running the complete post-effect recovery family, normal
direct/factory/filesystem/Hermes roots and persisted historical reload with
four isolated test workers and fail-fast. Log: /tmp/terminal-family-check.log.
The interrupted prior diagnostic run is 4 passed, 1 failed in 130.58 seconds,
not a completed gate. No source writer is active during this run.

The parallel family run completed with 10 passed and four failures in 301.75s.
The historical persisted reload and missing/stale/substituted observation cases
passed. All four failures share a new assertion-helper defect: it queried
`source_kind=preplanning_operation_control`, which is the inner kind, instead
of the canonical `semantic_ingestion_preplanning_control` source kind. The
source delta and outcome assertions preceding that lookup passed. Correct the
test lookup; do not change storage behavior. Remaining lost-ack/restart and
normal-root cases require completion. Independently, all 75 shared canonical
evidence-arena/native-codec/builder regressions pass in 94.93s.

After the test-helper correction, all 22 remaining lost-acknowledgement,
restart/reclaim and normal-root cases pass in 309.88s (`recovery-roots.log`).
All 18 assembler/observation-record checks pass in 5.98s; identity hygiene exits
zero. `source-candidate.json` freezes 20 source/test/fixture files and the
current binding map at SHA-256
1865701d4045bcfae13366c879beb545bbd01263f4ca1e4355c40dcb5e3256d9.
Independent spec and correctness reviews are active; the test reviewer is
queued because the collaboration tool reports its thread limit. No source
writer is active. This is a bounded debugging review, not observer/M5 closure.

Spec and correctness approve the frozen slice. Test review requests four
predeclared evidence families: observe actual terminal CAS membership; replay
historical V1 through the public root; corrupt persisted V2 bytes and reopen via
the public root; invalidate terminal authority after group completion. These
are confirmed bounded evidence actions. The review's P2 classifications are
reduced to Not applicable / changes_required / verification because no broken
product behavior is demonstrated; missing tests alone are not product priority.
The existing positive and direct-store proofs remain valid, but do not replace
these stronger public-boundary checks. One test-only evidence batch follows;
no product remediation or new external signing decision is justified.

## Public-Boundary Evidence Batch

Root ran the eight added cases with four workers. Five passed: actual same-CAS
membership plus scope/fence/lease/writer loss after group completion. Two persisted
corruption cases failed in fixture setup because `_record_store` is a bound method.
Using the real `_records` backend corrected both; both pass after reopen.
The historical case initially used the ordinary rather than captured scenario
host; restoring the same host still exposes only `admission.verification_digest`
as a mismatch (diagnostic preserves every original return/exception).

The isolated HEAD capture discovers one installed `memorii` distribution; the
current checkout discovers its local egg-info and site-packages entries. Bootstrap
component inventory deliberately selects distribution identity only for exactly
one entry. Captured/current profile comparison shows installed distribution vs
source-checkout identity; all eight fingerprinted source files are unchanged.
A bounded test-review consultation is checking faithful environment reconstruction
without weakening component/signature verification. Current test files changed;
old source candidate remains historical, not approval of this test delta.
Evidence: `public-boundary-initial.log`, `persisted-public-check.log`,
`historical-admission-diff.log`. Root is sole test writer; no production edits.

The reviewer approved a narrow package-inventory fixture overlay with real
version, module resolution, component bytes, HMAC and admission checks retained.
Root implemented that overlay plus the required duplicate-inventory negative.
Both pass in 12.52s, with the positive profile verification digest equal to the
captured admission digest before sync. The positive public replay performs no
normalization or graph execution and leaves the snapshot unchanged. No fixture
bytes were recaptured or changed. Ruff on all three test-delta files passes;
identity hygiene exits zero (`identity-final.log`).
The test-only successor candidate is
89907b04b2a9888ad6b708c859c3db1bde359dfb029dc24eed03a7780b80c9a5.
Every production hash matches the prior independently approved source candidate.
Targeted test review is active; parent observer/M5 closure remains excluded.
