# Static Native Decoder Table Slice

- Changed owners: `memorii/memorii/core/memory_evolution/ingestion_contracts.py` and `memorii/tests/unit/core/memory_evolution/test_native_observation_decoders.py`.
- Delivered: a cached immutable finite table of all 179 reviewed decoder IDs, each mapped by fixed local import to its native Pydantic `model_validate` callable. Exact-ID lookup fails closed; construction requires mapping-shaped fields and preserves native model validation.
- Construction authority: `decoder-owner-inventory.json` is review-only input used to author the fixed source table. Production code neither opens nor imports that inventory, uses reflection, nor provides a generic decoder fallback.
- Circular-import probe: a fresh process built the complete table successfully (`mappingproxy`, 179 members); imports remain inside the cached builder so `ingestion_contracts` itself does not eagerly load native model owners.
- Focused proof: table keys equal the complete 179-entry review-only inventory, mapping mutation fails, a `MemoryScope` example constructs through its exact ID, and `SourceRetentionTimeWitness`, `TransactionGroupCommitTimeWitness`, generic unknown, and empty IDs fail closed. Non-mapping fields fail and an extra `MemoryScope` field raises Pydantic `ValidationError`.
- Temporary construction proof: the test reads the review-only inventory solely to prove complete static-table coverage. Replace that input with the canonical published-package inventory when it exists; this slice neither duplicates that fixture nor broadens into package publication.
- Static typing: from `memorii/`, `../.venv/bin/pyright --pythonpath ../.venv/bin/python memorii/core/memory_evolution/ingestion_contracts.py tests/unit/core/memory_evolution/test_native_observation_decoders.py` completed with `0 errors, 0 warnings, 0 informations`.
- Binding status: no `production_entrypoint_bindings` update applies. The frozen ledger has zero production callers for this future registry API; this construction leaf does not claim source/binding/integrity admission, signature or self-digest validation, persistence, replay, or runtime closure. Parent milestone remains partial.
