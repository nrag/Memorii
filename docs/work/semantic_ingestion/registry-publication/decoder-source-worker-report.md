# Decoder Source Manifest Verifier Slice

- Owner: bounded decoder-source manifest worker.
- Changed owners: `memorii/memorii/core/memory_evolution/typed_value_decoder_sources.py` and `memorii/tests/unit/core/memory_evolution/test_typed_value_decoder_sources.py`.
- Delivered: immutable strict raw canonical JSON intake reusable for external manifest owners; exact profile-3 decoder-source manifest shape, literal profile identity, UTF-8 tuple ordering and duplicate checks; protected absolute canonical source-root descriptor-relative reads held from `/` through every `O_DIRECTORY|O_NOFOLLOW` component; nonblocking final opens before regular-file verification; normalized surrogate/recursion failure; strict UTF-8 whole-file validation and raw SHA-256 verification; shared-file consistency checks; deterministic canonical LP decoder-source snapshots using the existing `_length_prefixed` primitive.
- Verification: `../.venv/bin/python -m py_compile memorii/core/memory_evolution/typed_value_decoder_sources.py tests/unit/core/memory_evolution/test_typed_value_decoder_sources.py` passed from `memorii/`. `../.venv/bin/python -m ruff check memorii/core/memory_evolution/typed_value_decoder_sources.py tests/unit/core/memory_evolution/test_typed_value_decoder_sources.py` passed from `memorii/`. Root owns pytest and broader package gates.
- Focused proof authored: shared whole-file rows for two decoder IDs, deterministic distinct snapshots, path traversal/absolute/backslash/percent/Unicode/trailing-separator rejection, symlink rejection, raw file digest substitution, duplicate and shared-path metadata conflicts, strict raw manifest canonical/no-LF handling, escaped unpaired-surrogate normalization, nonblocking FIFO rejection, and invalid UTF-8 source bytes despite a matching raw SHA-256.
- Production entrypoint binding: no update is applicable. The frozen `production_entrypoint_bindings` ledger records zero callers for the shared observation/public registry API. This component verifies external construction inputs only; it has no decoder imports, runtime composition, persistence, transaction, or deployment caller.
- Explicit non-goals: registry compilation, publication-manifest joining, deployment pins, static decoder mapping, decoder import/selection, and production composition. This bounded construction slice does not close registry-publication milestone 1 or its parent milestone.
