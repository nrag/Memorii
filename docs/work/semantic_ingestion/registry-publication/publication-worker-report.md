# Typed Value Publication Join Slice

- Owner: bounded publication-manifest worker.
- Changed owners: `memorii/memorii/core/memory_evolution/typed_value_publication.py` and `memorii/tests/unit/core/memory_evolution/test_typed_value_publication.py`.
- Delivered: strict raw publication-manifest intake; exact profile-3 source-role and decoder-snapshot ordering/identity checks; canonical LP publication digest; raw declaration recompilation plus decoder-source recomputation; source-role SHA, decoder snapshot, registry digest, and decoder-entry closure joins; immutable protected publication/deployment pins including raw independent-vector-manifest SHA-256 verification.
- Verification: `../.venv/bin/python -m py_compile memorii/core/memory_evolution/typed_value_decoder_sources.py memorii/core/memory_evolution/typed_value_publication.py tests/unit/core/memory_evolution/test_typed_value_decoder_sources.py tests/unit/core/memory_evolution/test_typed_value_publication.py` and the matching focused Ruff command passed from `memorii/`. Root's focused source/publication pytest suite passed 21 tests in 4.53 seconds. Root's configured `pyright --pythonpath ../.venv/bin/python` check reported zero errors including tests. Broader compiler-dependent gates remain pending.
- Focused proof authored: valid recomputed temp-file source package join; unknown role, source-file hash, source-file order, and decoder-snapshot mutations; changed raw decoder manifest digest with unchanged snapshot data; publication, vector, registry, and snapshot deployment pin mismatch paths; actual decoder source-byte change that preserves schema `binding_digest` while changing its source snapshot, decoder digest, entry digest, and publication digest; and rejection of mutable snapshot-pin lists, malformed snapshot records, non-string digests, and malformed nested protected limits.
- Production entrypoint binding: no update is applicable. The frozen entrypoint ledger has zero callers for this future registry path, and this helper cannot publish a usable registry, load a decoder, or perform persistence/composition.
- Explicit non-goals: runtime decoder table, registry history, persistence, provider composition, live publication, and independent-vector content/parity certification. This construction result does not make a usable registry available and does not close the parent milestone.
