# Observation Ledger Contract Slice

- Owner: bounded observation-ledger contract worker.
- Changed owners: `memorii/memorii/core/memory_evolution/observation_ledger_contracts.py` and `memorii/tests/unit/core/memory_evolution/test_observation_ledger_contracts.py`.
- Delivered: strict, frozen, extra-forbidden profile-3 shapes for ledger heads and entries; group/source result locators with a closed discriminated alias; revision-free terminal-group/source-finalization semantic payload roots and alias; source-finalization intent; and ledger activation. The payload roots copy the native delta fields exactly after removing only `observation_revision_before`, `observation_revision_after`, and `delta_digest`; neither payload nor intent gains a `schema_version` body field. They do not add a wrapper result digest or implicit fields.
- Shape proof: sequence-zero/one predecessor grammar, explicit required-nullable head/entry predecessor fields, boolean schema-version rejection, group operation-coordinate ordering/uniqueness/nonempty identifiers, payload field-set equality against native model fields, valid-native delta conversion preserving every retained field, discriminated-union rejection, strict activation types, locator/delta kind coupling, and source/group coordinate joins are encoded in the contracts and focused tests. The module calls the canonical `rebuild_graph_effect_contracts` owner before exposing ledger unions; a fresh-process `TypeAdapter(ObservationLedgerSemanticPayload)` construction succeeds without test-only initialization.
- Static evidence: `PYTHONPATH=memorii .venv/bin/ruff check memorii/memorii/core/memory_evolution/observation_ledger_contracts.py memorii/tests/unit/core/memory_evolution/test_observation_ledger_contracts.py` passed. `PYTHONPATH=memorii .venv/bin/pyright --pythonpath .venv/bin/python memorii/memorii/core/memory_evolution/observation_ledger_contracts.py memorii/tests/unit/core/memory_evolution/test_observation_ledger_contracts.py` passed with `0 errors, 0 warnings, 0 informations`. A fresh import plus `TypeAdapter(ObservationLedgerSemanticPayload)` succeeded. `git diff --check` passed. Root owns pytest and broader gates, so this worker did not run pytest.
- Production entrypoint binding: no `production_entrypoint_bindings` update is applicable. This is a shape-only leaf with no production caller, protected registry decoder, store CAS, persistence, replay, or composition root. It makes no runtime or persistence closure claim.
- Explicit non-goals: registered CTV body decode and self-digest verification; genesis revision value and head/entry ID computation; protected native record-ID recomputation; immutable result/receipt joins; registry publication; source grammar-3 receipt assembly; activation transaction/fencing; ledger append/replay; and host composition. Those owners must establish the required non-test production caller and update `production_entrypoint_bindings` before any runtime/persistence requirement can close.
- Parent status: this bounded slice is complete only as local contract construction. Registry-publication milestone 1 and the parent authenticated observer/comparator milestone remain partial.
