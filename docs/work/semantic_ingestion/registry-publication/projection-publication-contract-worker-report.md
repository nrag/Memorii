# Native Projection Publication Contract Slice

- Owner: bounded native projection-publication contract worker.
- Changed owners: `memorii/memorii/core/semantic_ingestion/bootstrap_graph_projection_publication.py` and `memorii/tests/unit/core/semantic_ingestion/test_bootstrap_graph_projection_publication.py`.
- Delivered: strict frozen extra-forbidden profile-3 body shapes for `BootstrapGraphNativeProjectionPublicationReceiptV3`, `BootstrapGraphNativeReplayAuthorityEvidenceV3`, and `BootstrapGraphNativeReplayCheckpointEvidenceV3`. The receipt retains full native graph/event/projection/publication binding values; evidence roots retain the full aggregate or checkpoint bundle. Every declared field is required, schema-version booleans reject, and all digest coordinates use lowercase SHA-256 lexical validation.
- Deterministic coordinates: `publication_identity_digest` implements the approved six-member LP preimage; the three exported immutable-member ID functions emit the exact receipt, aggregate, and checkpoint IDs. The direct coordinate helper requires its `receipt_id` to equal the exact deterministic receipt ID, then checks receipt/evidence identity, digest, replay-binding, post-graph-revision, and retained-checkpoint joins without trying to validate protected self-digests. This rejects coordinated substitution of both evidence `receipt_id` fields with the same arbitrary value.
- Static evidence: `PYTHONPATH=memorii .venv/bin/ruff check memorii/memorii/core/semantic_ingestion/bootstrap_graph_projection_publication.py memorii/tests/unit/core/semantic_ingestion/test_bootstrap_graph_projection_publication.py` passed. `PYTHONPATH=memorii .venv/bin/pyright --pythonpath .venv/bin/python memorii/memorii/core/semantic_ingestion/bootstrap_graph_projection_publication.py memorii/tests/unit/core/semantic_ingestion/test_bootstrap_graph_projection_publication.py` passed with `0 errors, 0 warnings, 0 informations`. A fresh real typed fixture assembled canonical projection publications, graph/event state, checkpoint bundle, aggregate, receipt, and both evidence roots; its direct coordinate join accepted. `git diff --check` passed. Root owns pytest and broader gates, so this worker did not run pytest.
- Explicit non-goals: registered profile-3 body decoding and receipt/evidence self-digest verification; group-result/reload schema changes; projection-history preparation or validation beyond typed-owner values; atomic CAS writes/reloads; immutable-member serialization; replay reconstruction; registry publication; and production composition. This contract leaf creates no persistence or runtime claim and does not update `production_entrypoint_bindings`.
- Parent status: this bounded model inventory slice is complete only as local contract construction. Registry publication, protected decoder registration, native group CAS/reload, and the parent authenticated-observer/comparator milestone remain partial.
