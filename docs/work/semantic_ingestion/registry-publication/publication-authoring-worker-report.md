# Typed Value Publication Authoring Slice

- Owner: bounded publication-package authoring worker.
- Changed owners: `memorii/memorii/core/memory_evolution/typed_value_publication_authoring.py`, `memorii/memorii/core/memory_evolution/typed_value_decoder_sources.py`, `memorii/memorii/core/memory_evolution/typed_value_registry_compilation.py`, `memorii/memorii/core/memory_evolution/typed_value_publication.py`, and feature-local authoring tests.
- Delivered API: `author_typed_value_publication_package` accepts already-read non-decoder/non-registry declaration bytes, finite typed `DecoderSourceSelection` rows, an absolute protected source root, protected limits, and generated-file exclusions. It captures selected UTF-8 whole files through the descriptor-relative nofollow/capped reader; emits canonical decoder-source-manifest bytes; derives source snapshots; generates normative decoder declarations; derives the registry role through `author_typed_value_registry_role`; compiles it; and emits canonical publication-manifest bytes. The returned package retains only immutable tuples and frozen records.
- Fail-closed behavior: supplied decoder or registry roles, missing/extra decoder selections, malformed raw declarations, traversal/generated selections, duplicate selection identities, source races or byte substitutions, malformed role closure, and manifest failures reject. The verifier re-reads and re-hashes every shared source row, then shares an identical immutable bytes object after equality succeeds so a large shared native source is not retained once per decoder.
- Verification: `../.venv/bin/python -m pytest -W error tests/unit/core/memory_evolution/test_typed_value_publication_authoring.py tests/unit/core/memory_evolution/test_typed_value_publication.py tests/unit/core/memory_evolution/test_typed_value_decoder_sources.py -p no:cacheprovider` passed (25 tests, 5.00 seconds) from `memorii/`. Explicit Ruff and `py_compile` for changed production/test files passed.
- Evidence delivered: deterministic repeated authoring, full author-to-verifier round trip using explicitly test-only vector bytes, supplied role/selection closure rejection, traversal rejection, post-authoring source tamper rejection, and shared-row byte-object reuse.
- Production entrypoint binding: no update applies. This offline authoring owner has no non-test production caller, runtime decoder table, persistence transaction, deployment authority, or activation path. Parent registry-publication requirements remain partial.
- Remaining limits: this does not author or validate an independent vector manifest, generate deployment pins, prove independent parity, freeze the actual source package, activate publication, load decoders, or expose a registry through production composition.
