# Operational Profile Registry Publication

- Current resume: resume.md
- Work type: implementation
- Status: paused for dependent ledger runtime integration
- Parent: ../engineering-closure/milestones/05-authenticated-observer-comparator.plan.md
- Approved design: ../observation-ledger/design-candidate-final.json,
  215ab5f272d7b27589da04c9c1dd4a9fa0f1d8f01413d542ea72bffa4c924b9e.
- Baseline: semantic_ingestion_m5, HEAD191826cd3afb38bf605a337a71d576063b3bae5e,
  authorized dirty tree. Preserve all prior and unrelated scoped-context changes.

## Objective And Boundaries

Implement the protected operational profile registry and its published source
package, independent compilation evidence and production composition. Governing
contracts are SIA plus docs/design/semantic_ingestion_observation.md. Preserve
profile-2 historical bytes and the fixed nonoperational 56-root compiler.
This plan does not implement shared ledger transactions or public retrieval;
it supplies their verified registry prerequisite. Parent requirements remain
partial until those runtime paths and their proof are complete.

## Milestones

1. Implement the exact raw declaration parser, finite typed schema/policy models,
   source/decoder manifest verification, LP compilation and immutable registry
   construction in the canonical ingestion-contract ownership boundary. This
   construction work alone is not production publication or milestone closure.
2. Author the full source-selected observation model inventory and declaration
   package, static decoder mapping and immutable source snapshots; verify exact
   role closure and emit the publication/pins without self-hashing constants.
3. Independently compile the frozen raw package under the design's prohibited
   shared-input rules, compare complete outputs/negative families, and compose
   the same verified registry through provider capability, writer/store and
   future replay/observation configuration. Only actual callers count as binding.

## Verification And Evidence

Use ../observation-ledger/registry-verification-matrix.md and
entrypoint-verification-matrix.md as the predefined attack/proof portfolio.
Root owns pytest, package gates and long processes. Worker runs focused static
checks only. Keep pure grammar/digest tests in unit and source-package/process/
production composition tests in integration. Final checks include applicable
workflow jobs, source chain, Ruff, configured Pyright, identity hygiene and
three independent roles at a frozen coherent candidate. Record current revision,
file hashes and actual command outcomes; do not claim CI from local execution.

The Spark production map is ../observation-ledger/entrypoint-preflight.md;
readiness.md records exact future provider/capability/atomic owner arguments.
The configured registry verifier has one production callsite in the built-in
capability; the integrity helper calls the reader, but neither it nor the candidate encoder has a reachable public runtime owner.
The canonical parent production_entrypoint_bindings ledger records construction
proof separately from missing artifact/ledger/read use.

## Ownership, Identity And Budget

Coordinator owns scope, plan, tests and final evidence. One Terra worker owns
canonical registry implementation and feature-local tests. Other writers may
only own disjoint retained-evidence files. Symbols and artifact names describe
profile, declaration, registry, publication and decoder behavior; delivery and
requirement coordinates occur only in traceability metadata. Preserve existing
package boundaries; do not add untyped payload dictionaries, reflection-based
schema discovery, caller-selected decoders or legacy-profile fallbacks.

Budget: one bounded construction pass per milestone, one full coherent review,
and one consolidated correction of confirmed defects/conformance obligations.
External semantic ambiguity stops dependent work with the exact unresolved
contract; ordinary deterministic implementation choices need no user approval.

## Next Action

Refresh registry/runtime evidence after the dependent ledger activation slice
in ../observation-ledger/milestones/activation.plan.md.

## Resumed Independent Work

The coordinator's previous turn stopped too broadly at a numeric-only decision.
The user challenged that stop; unaffected source/model construction resumes.
The original Terra ledger writer owns a disjoint new replay/checkpoint contract
module, feature-local tests and report, using the approved exact field inventory.
It must preserve native nested validation and defer registered self-digests to
the future decoder. Spark owns a read-only public snapshot/cursor/page conformance
map for the next bounded handoff. Root owns all pytest, documents and reconciliation.
Neither assignment depends on numeric wrapper encoding. No acceptance or runtime
completion is inferred from their model construction.

The numeric amendment is now specified and independently approved at candidate
2ad0df654836d87b72304a259265105075589a3153c34dc2054a46405e314d1d;
../numeric-wrapper-wire/closure.md records observation-only promotion and the
unchanged SIA/CTV/structural/workflow hash proof. The Terra ledger writer now owns
only declaration/compiler numeric changes, a new typed numeric-value owner and
their feature-local tests. This changes the earlier construction evidence's
parser/compiler hashes; that evidence is historical until rerun at the new files.

Replay/checkpoint and ingestion-time model construction passes ten focused tests
in 5.96s after UTC and tail-revision conformance corrections. Exact evidence is
replay-time-model-verification.json. Public snapshot/cohort/cursor construction
passes an initial ten tests with the historical foundation suite; its additional
exact-integer schema-version guard passes three focused tests in 4.45s. The same
Terra payload writer now owns a disjoint public page/request/retained-snapshot
model module. Old cursor wire modules remain unchanged. Root owns pytest.

The public conformance mapper's missing SIA field claims were unsupported: root
located the exact time attestation/page declarations and corrected the map's
appendix. That artifact is navigation only and cannot supply schema authority.

## Construction Delegation

The initial combined parser/compiler/publication task was too large for the
prior worker's retained context; it produced no code. The coordinator split it
without changing semantics. Fresh Terra worker operational_declarations owns
only typed_value_declarations.py and its feature-local tests: complete strict
raw intake/typed role IR and role-local invariants. Cross-role closure, LP
compilation, source publication and production composition remain the next
dependent constructions. Spark maps existing versus missing root-model owners
in model-inventory-map.md. Root owns tests and all closure judgments.

The raw parser now passes 12 focused tests under root Python 3.12.14, pytest
9.0.3, warnings-as-errors (4.34 seconds). The final cases cover exact positive
integer resource limits, immutable byte intake and node-budget precedence.
The same Terra worker now owns disjoint registry compilation files, consuming
raw declarations and enforcing cross-role closure and all LP commitments.
A fresh Terra worker owns only decoder-source manifest verification and its
tests, allowing independent source-path verification work in parallel without
overlapping edits. Reports are declaration-worker-report.md and
decoder-source-worker-report.md. No construction result is production binding.

The bounded spec consultation resolved the source-status question: the initial
publication's entries compile with the normative literal `active` (Publication
Rule), while activation controls writer permission. `replay_only` and `retired`
are exact retained-history statuses; this compiler does not invent a role or
infer a transition. This is implementation conformance, not a design change.

Construction verification found 14 parser/compiler tests passing, but the first
compiler candidate had 73 explicit-file Pyright diagnostics. The coordinator
also identified determinate closure corrections: empty ordinary models,
misowned/nested numeric wrappers, and distinct entry versus closure ordering.
The sole compiler writer is correcting these together and adding discriminating
closure tests; no candidate was frozen or approved from the initial checks.
The first decoder-source candidate passes 12 tests and explicit-file Pyright.
Coordinator inspection identified bounded error handling and filesystem intake
corrections (surrogates, recursion, FIFO and root-descriptor traversal), which
remain with its sole writer. That writer also owns the disjoint publication
manifest join. Source and registry verification are still construction evidence.

The bounded native-declaration map is native-declaration-compatibility-map.md.
Coordinator corrections preserve exact tuple/enum encodings and require an
explicit declaration of the closed SnapshotGraphRecord payload union. Native
decoder validation never substitutes for complete schema declarations.

The compiler writer completed static corrections but left the checkpoint
preimage proof unfinished. A fresh Terra worker, registry_compiler_proof, now
solely owns that compiler/test boundary to complete the finite remaining cases
and the decimal-bound lexical conformance documented in
numeric-source-consult.md. No generic numeric range rule or rounding permission
was invented from a field name.

A separate Terra worker, observation_payload_contracts, owns only the new
graph_observation_records.py structural payload models and their local tests.
This advances the approved source inventory while construction checks finish.
It preserves native nested validators and models the new public payload shapes;
profile-3 self-digest verification remains the future protected body decoder's
responsibility. It must not substitute legacy digest algorithms or claim a
runtime observation caller. The root owns all pytest processes and evidence.

## Current Construction Checkpoint

The four declaration/compiler/source/publication construction modules now pass
65 tests and explicit Pyright; construction-verification.json pins their exact
files. This is not registry publication. Structural and ledger models plus the
existing observation foundation/persistence tests passed 26 tests in 5.53s.
Ingestion payloads, 17 explicit stream alternatives and native publication
receipt/evidence shapes passed 10 tests in 5.88s. Explicit Pyright for the four
new observation/ledger model modules and tests reports zero diagnostics.

Coordinator inspection removed an incorrect equality between delivery and
authenticated source-reference keys; the corrected positive fixture deliberately
uses different keys. Native receipt/evidence join coverage exposed a missing
deterministic receipt-ID check. Its sole original writer corrected that check
and added real native positive/substitution cases. Root verification passes
seven tests in 6.15s with warnings-as-errors; the separate exact file evidence is
native-publication-model-verification.json. This remains local model and join
evidence, without a protected self-digest decoder or transaction integration.

The inventory map now identifies 57 constructed profile-3 model rows, including
replay/checkpoint/page/snapshot models. These remain unpublished. The draft
source inventory additionally includes existing native roots and helpers.

A separate design prerequisite, ../numeric-wrapper-wire/design.plan.md, records
two genuine missing persisted contracts: numeric wrapper wire shapes and the
decimal encoding-spec ID source. Its existing-map proposal passes 5 positive
and 14 negative feasibility cases. The user approved that choice on 2026-09-07;
the frozen amendment passed independent review and was promoted, as recorded
in ../numeric-wrapper-wire/closure.md.
Unaffected construction continues. This is separate
from the resolved numeric lexical/source consultation and the already approved
separate temporal/trust projection semantics.

## Current Source And Body Validation Work

The user-approved canonical map and decimal encoding-policy choice is fully
resolved. Numeric/public model verification records 36 passing tests in 5.72s
in numeric-public-model-verification.json. This supersedes initial numeric
construction claims only for its pinned files and selected test scope.

The offline source authoring aid produces an explicitly untrusted draft: 78
static roots and 180 schemas. Initial audit reports 146 integrity-policy rows,
78 unsupported annotations, 12 union discriminator rows and one enum identity.
These are authoring tasks, not automatically external semantic blockers.
A bounded spec consultation permits reviewed model inspection only in the
offline drafting aid; compilers and runtime still consume frozen raw sources
and a finite static decoder mapping.

Delegation: Terra observation_payload_contracts owns the draft tool, its tests,
and generated review artifact, now resolving nested annotation and existing
discriminator metadata. Spark source_policy_audit is read-only, mapping native
ordinary integrity fields versus new profile-3 policies. Fresh Terra
schema_body_validator owns only typed_value_body_validation.py and tests; the
coordinator requested closure-scoped enum lookup and portable early-year UTC
validation after inspecting its first candidate. These disjoint assignments
advance source publication without changing approved persisted semantics.

Initial body-validator and authoring-tool checks: nine tests passed in 5.45s
under root Python 3.12, warnings as errors. These files are now under bounded
correction, so the initial result is historical construction evidence, not
final candidate evidence. No production caller or registry publication exists
from these checks. Root continues to own pytest and broad gates.

Terra registry_role_authoring owns only the existing registry compiler and its
feature-local tests: derive the raw registry declaration from complete reviewed
non-registry roles through shared validated compilation preparation, then verify
that result through the regular compiler. This closes the package builder's
private-helper dependency; it is explicitly not independent compilation.
The body validator's enum closure correction also follows the finite external
checkpoint preimage dependency, matching the compiler's complete closure rule.

## Verified Authoring Checkpoint

The corrected source-directed body validator passes six focused tests in 5.35s
(body-validation-verification.json). Registry-role authoring and publication
construction pass 43 tests in 5.61s (registry-authoring-verification.json).
Source drafting resolves all 78 initial annotation and all 12 union rows,
including existing publication_kind literals on native certificates. Remaining
initial audit: 146 integrity policies and one explicit enum registration.

Coordinator reconciliation rejects the Spark source-policy bucket map as
unsupported: it incorrectly classified explicit ledger/public self-digest roots
as ordinary and the digest-free cohort preimage as self-digest. No source role
is generated from that map. The Terra source writer now authors a finite
review-only policy proposal from the verified normative tables and an explicit
SourceModality enum declaration. Native embedded integrity remains ordinary;
new profile-3 self-digests remain distinct. Unspecified domain literals are
explicit source proposals for review, not requests for another wire decision.

Terra spec_auditor time_policy_consult is read-only and bounded to the three
ingestion-time models: determine whether the closed policy grammar covers the
attestation and signed witness contracts. This does not reopen R14 or authorize
release signatures. Actual contradictory signing semantics, if confirmed, will
be recorded separately from routine source authoring choices.

The bounded time-policy consultation confirms a determinate higher-authority
conflict: SIA31084 and33774 assign signed witnesses exclusively to acceptance,
while the observation production inventory erroneously includes one. A linked
design operation, ../time-witness-boundary/design.plan.md, freezes the narrow
exclusion amendment for three-role review. Production attestation schema fields,
public union and R14 remain unchanged. The source writer continues disjoint
ordinary/native and attestation integrity declaration authoring; no production
witness cleanup occurs before amendment promotion.

Body-test fixtures now use the public registry author instead of compiler-private
helpers; the refreshed six-test result is 4.59s with updated hashes in
body-validation-verification.json.

## Production Witness Exclusion And Static Decoder Construction

The bounded witness-boundary design is approved and promoted. The production
model/export/draft root is removed; both witness kinds are excluded from the
source inventory. Current inventory: 77 explicit roots, 179 root/helper schemas.
All 895 proposed schema/enum/optional/numeric/integrity roles parse after
test-only removal of proposal-review metadata. The draft remains untrusted.

Source optional policies now match complete native emitted bodies: required or
required_nullable, never omission inferred from a Python construction default.
Generic grammar support for explicit future omittable policies is unchanged.
The initial combined public negative test was too broad to prove individual
extra-field rejection; coordinator required exact paired witness shapes and
one-field mutations at both snapshot/page boundaries.

A proposed full codec/table assignment was not implemented; its owner requested
a source-first split. Coordinator rejects a circular requirement to verify the
complete source package before the decoder files exist to hash. Instead, the
explicit review-only decoder-owner-inventory.json maps all 179 current native
owners and normative decoder IDs, with no fabricated implementation hashes.
Fresh Terra static_native_decoder_table owns only a finite code-owned table in
ingestion_contracts.py and focused tests. It uses fixed imports, no runtime
reflection/dynamic imports or draft-tool dependency. Publication verifies exact
source identities after stable code and independent source audit exist. Root
owns all source-chain refresh, tests and closure claims.

The consolidated source/witness/public-contract check now passes eleven tests
in 5.75s, with exact final hashes in source-and-witness-verification.json. It
includes all 895 proposed role parses, paired witness exclusion, exact raw
witness rejection and isolated authority/fixture extra-field mutations.

Fresh Terra native_model_materialization owns only a new model-codec module
and tests, consuming the finite table's agreed API. Its slice is source-directed
native conversion plus exact reencoding after verified publication identity
joins. It explicitly does not claim signature/self-digest or external native
authority authentication from model_validate. Those required policy checks and
outer binding/history parser remain dependent protected-decoder work. Root
continues source/package construction and verification.

## Publication Authoring And Decoder Proof Checkpoint

The coordinator authored 895 raw schema/policy roles from the reviewed draft,
plus 179 null-only upcast roles and the fixed grammar, under the production
observation_registry_sources directory. These 1075 files are construction
inputs; decoder roles, registry and publication manifests remain unissued.
raw-source-authoring.json and source-audit-candidate.json pin the bounded
895-role audit. Independent spec consultation found no concrete conformance
gap across the 179 native schema owners. This is not publication approval.

The finite 179-entry native table and historical ingestion-contract tests pass
14 tests in 5.48s. Publication package authoring, source verification, manifest
verification and registry compilation pass 60 tests in 5.40s; the exact current
file hashes and command are publication-package-authoring-verification.json.
The builder captures actual whole source files and emits derived bytes; it
does not manufacture deployment pins or independent parity claims.

Native conversion construction remains under verification. Its prior writer
completed source joins, nested dispatch, enum wire checks and bounded conversion
changes, but left real nested/enum fixture proof incomplete. Fresh Terra worker
nested_codec_proof owns only the original codec test file and its report to
finish that predefined evidence action. Spark decoder_source_closure performs
read-only offline native-validator/encoder closure mapping, without changing
runtime source selection. Root owns pytest, source-package emission and claims.

Coordinator inspection also identified determinate encoder conformance gaps:
map keys use raw UTF-8 order instead of encoded JSON-string byte order, invalid
native integers can emit an empty string, and some encoding allocations precede
resource checks. Fresh Terra native_encoder_conformance owns only the codec
production file, separate emission tests and report for one coherent bounded
emission correction. This is construction conformance, not a shipping-priority
claim from hypothetical inputs. Do not freeze its source snapshot until the
batch and required proof are reconciled. No user decision is pending.

Fresh Terra protected_registry_history owns only a new immutable history module,
local tests and report. The already approved history/status matrix governs
complete coordinate conflict rejection, exact embedded binding resolution and
original publication retention. This slice does not invent an activation
token or expose writer permission; protected service composition remains later
work. It is disjoint from native codec and source files. Root owns integration,
pytest and the final atomic publication judgment.

Fresh Terra independent_registry_compiler owns only a new acceptance reference
compiler, its local tests and report. Its allowed shared inputs are normative
design/LP definitions and frozen raw declaration/source files. It must not read
or reuse production codecs, parser/compiler/model code, source-draft reflection,
old acceptance CTV helpers, or generated expected outputs. The 1075 current
declaration hashes are separately inventoried in source-role-inventory.json;
decoder/registry roles will be supplied only after source freeze. Independent
parity and its vector manifest remain unproved until both implementations run
against the same complete frozen input and their full output bytes agree.

The first source-closure mapper response did not answer native helper closure
and mislabeled the legacy semantic-contract codec as operational profile 3.
Coordinator rejects those claims. The mapper has one bounded correction to
trace actual native validator/encoder helper symbols. A coordinator offline
conservative import probe reaches 89 files including unrelated store/provider
paths; that probe is not a selected manifest or runtime import discovery.

## Complete Source Candidate And Current Proof

The coordinator completed an offline symbol-level source trace: 179 native
class seeds plus body/materialization/publication functions reach 671 symbols
in 32 files, with zero unresolved symbols and no local module-alias/relative
imports. The mapper's correction still contained nonexistent paths and invented
call signatures; it is rejected as source authority. decoder-source-selection.json
records the coordinator's explicit finite file list. A fresh spec_auditor
decoder_snapshot_review checks that frozen selection as a bounded consultation.

publication-candidate-construction.json records successful construction from
actual source bytes: 1255 declaration roles, 179 schema entries, 5728 shared
decoder-source rows selecting 32 whole files, plus both external manifests.
Construction took 9.051s. No deployment pins, vector approval or activation were
fabricated. The ordinary authoring builder is the generator; the source draft
and runtime reflection do not participate in compilation or source selection.

The native codec/body/table/history combined check passed 36 tests in 6.46s.
Coordinator then required history's exact schema-to-decoder mapping and source
manifest digest join. The corrected history passes ten tests in 5.23s; its
decoder substitution test now expects the earlier precise rejection stage.
A native-table test import-order finding was corrected. These are local
construction checks, not final candidate gate or runtime evidence.

The independent compiler initially passes three local tests and compiles all
1255 actual roles, reproducing profile, registry and publication digests in
5.421s. Coordinator inspection found incomplete exact-grammar/role/numeric
admission and missing explicit resource controls, so this positive result is
not independent-parity closure. Its sole writer is completing one consolidated
normative admission/rejection-family batch without production-code inputs.

At the user's request, the all-23 table in
../../semantic_ingestion_completion_readiness/closure-plan.md now explicitly
records 15 retained baseline-complete requirements, six partial, one blocked
and one not implemented. All eight open requirements and final CI/review remain
visible. The table is a progress crosswalk, not final engineering approval.

## Verified Construction Checkpoint

DREV-001 is a confirmed `Not applicable / changes_required` source-selection
conformance finding: generated files must be unconditionally ineligible, not
dependent on optional caller exclusions. The bounded correction reserves the
generated namespace, rejects a source root inside it, and validates supplemental
exclusions before opening descriptors. The source/authoring/publication check
passes 36 tests in 5.24s. The same spec auditor approves this delta with no
remaining concrete gap; it is not whole-publication approval.

The source-code change invalidated the initial candidate's decoder snapshots.
publication-candidate-initial.json preserves its metadata. The regenerated
candidate is publication-candidate-construction.json, with registry digest
050ef1003458315053ecc8c51b34418206288116f15032e5edccbeee7c602079.
reproduce_publication.py verifies checked-in generated bytes by default;
--refresh-generated only rebuilds construction outputs and never issues pins or
activates a registry. Independent normalized profile/entry/registry preimage and
output bytes agree for all 179 entries and 1255 roles. The exact output and input
identities are independent-output.json and independent-positive-parity.json.
Complete rejection-vector publication is still required before parity closure.

The consolidated construction check passes 168 tests in 6.42s. Scoped Ruff and
configured Pyright pass; registry-construction-checkpoint.json pins files,
commands, tool versions, resolved check failures and remaining work. Acceptance
test imports use a scoped Pyright environment matching their pytest root path;
the production environment is unchanged. The rebuilt wheel contains all 1257
registry JSON files byte-identically and excludes acceptance/tests. This local
no-build-isolation check is explicitly narrower than the workflow package gate.
No CI, final reviewer cohort, deployment, ledger or retrieval closure is claimed.

## Independent Rejection And Protected Envelope Work

The user authorized continuation on 2026-09-07. The construction checkpoint
remains the baseline; no new release or runtime-completion claim is made.

Delegation/cost ledger:

| Task | Role and ownership | Consumer and status |
| --- | --- | --- |
| registry_rejection_vectors | Terra worker; sole writer of acceptance/observation_registry_vectors.py and its feature-local test | Author finite independent raw-input and expected-output vectors from normative documents, raw declarations and the independent compiler only; active. Production implementation, callbacks, fixtures and generated expected outputs are prohibited inputs. |
| protected_envelope_matrix | Terra test_reviewer; read-only bounded consultation | Check existing fixed-envelope matrix before protected parser implementation; active. No full candidate approval or broad tests. |

Coordinator read-only inspection identifies two independent source-intake
questions for concrete vectors: symlink checks after path resolution and the
reserved generated-source namespace. They are not yet classified from executed
evidence. The existing production composition root is confirmed in capability.py:
it constructs writer admission and atomic store together, but neither yet
receives the new registry. The protected parser is a necessary prerequisite;
the legacy decode_artifact path decodes the body before verifying digests and
cannot be reused as the profile-3 authority boundary.

The coordinator's executed source-intake probe confirms both suspected
acceptances in the independent compiler while retaining the native-file
positive. independent-source-intake-probe.json records exact raw manifest/source
bytes and the compiler hash. Classification: `Not applicable / changes_required /
verification`, bounded `contract_conformance_action`; no operational product
impact is asserted. Correct the whole filesystem intake boundary with canonical
root/no-follow traversal and generated-file exclusion after the independent
vector set enumerates its sibling forms.

The envelope matrix consultant initially requested a nonexistent implementation
freeze. Coordinator classifies that finding unsupported: this is the required
preimplementation matrix consultation, not candidate approval. The same reviewer
has a bounded correction to evaluate the stable planned matrix against the
normative contract.

The corrected envelope consultation identifies four determinate evidence
actions: stage-specific real-delegate counters, an independent literal wire
oracle, separate read-status versus writer-permission proofs, and an explicit
unauthenticated materialization result. Coordinator accepts all four and adds
them to the fixed-envelope matrix. They do not change product semantics.

protected_artifact_reader is a Terra worker with sole ownership of the new
typed_value_artifact_reader.py and its feature-local tests. It implements the
bounded outer/history/digest boundary and subsequent selected conversion;
registered integrity, persistence and production composition remain later
requirements. This work is independent of the vector author's acceptance-only
files and algorithms. No overlapping writer exists. Root owns tests, evidence,
source-selection refresh and generated-output reconciliation. The new reader
must join the explicit decoder-source closure before publication proof is
refreshed; its implementation cannot preserve an outdated source snapshot.

The first independent corpus execution produced 53 vectors and seven failures:
five independent filesystem acceptances and two malformed positive decimal
fixtures. registry-vector-results.json is the latest execution output;
independent-source-intake-probe.json preserves the original reader evidence.
The bounded spec consultation confirms that decimal declaration bounds require
the declared scale, and that generic bound ordering must not be invented by
this registry compiler. The primary lexical rejection is correct. Independent
fixtures/compiler require a consolidated closed numeric-lexical correction;
the earlier inverted-range rejection expectation is unsupported. Classification
for both numeric findings is `Not applicable / changes_required / verification`.

independent_registry_conformance is the Terra acceptance-only writer for the
compiler, vectors and their feature-local test. Its allowed inputs remain
normative documents, independent modules and raw probe bytes; production
implementations and the comparison runner are prohibited. It owns the closed
filesystem and numeric-lexical corrections. Root owns the primary comparison
runner, vector execution and evidence. A new independent writer is used because
the previous completed agent could not be resumed under the tool's thread limit.

The artifact reader worker delivered a partial matrix despite reporting local
completion. Coordinator rejects that claim: it omitted several prescribed
stage, malformed-envelope, digest-value, resource and legacy proofs. Attempts
to resume or replace that worker were rejected by the agent thread limit, so
the coordinator is the sole writer for this bounded completion. The reader now
reuses the established native artifact_preimage (which already accepts profile
3) and handles large unknown version strings without Python's decimal digit
conversion limit. The completed local envelope matrix passes 55 tests in
5.24s. Source selection/publication refresh and targeted review remain required;
neither test success nor the worker report closes the parent requirement.

The consolidated independent correction now passes 58 construction vectors with
both implementations agreeing, and the reader/compiler combined check passes
107 tests in 5.46s. Configured Ruff/Pyright pass. The independent root reader
walks every root/path component without symlinks; a parent-alias vector and
ancestor-symlink unit case prevent final-component-only enforcement. Decimal
bounds use exact declared scale without Python large-integer conversion or a
new generic ordering rule. Initial failed vectors/results are preserved in
registry-vector-manifest-initial.json and registry-vector-results-initial.json.

The envelope consultant's three finite evidence additions are implemented:
literal wire/digests, prior-publication selection after history append, and
strict scalar/integer/default-omission negatives. Updated file pins and combined
execution evidence are in protected-envelope-verification.json; targeted delta
consultation remains to be reconciled.

The source candidate now selects 34 files and 6086 decoder-source rows. The new
reader and existing immutable history are explicit additions to the prior
32-file selection, not runtime import discovery. Regeneration succeeds; complete
independent output equality holds for all 179 entries/1255 roles at registry
fc6708f64f340818b3b38f1434e7a945fe4e63801cfd53a3392544007f2e78f9.
publication-candidate-before-artifact-reader.json preserves the superseded
metadata. Old wheel/construction-checkpoint evidence remains historical until
the affected package checks run again.

registry_runtime_composition is a Terra writer for protected configuration,
capability, paired writer/atomic store, provider reference propagation and their
feature-local tests. It must use the existing ordinary service construction
root, verify configured publication before partial construction, and share the
same immutable history. Existing unconfigured pre-activation behavior remains
valid; registry presence alone cannot authorize ledger activation. Root owns
evidence and production binding ledgers; no worker edits them concurrently.

The final reader test delta is approved within its explicit helper scope. The
combined vector manifest now commits both the 58 finite construction cases and
the full 179-entry/1255-role package's independent expected output and source
identities. verify_registry_vectors.py recomputes both implementations against
those expected bytes; all agree. The initial full-package run correctly rejected
the harness's small synthetic source limit. The harness now uses the separately
declared complete-package limits already used by reproduce_publication.py;
synthetic limits and their rejection tests are unchanged.

The manifest digest is
c628efcad726d6a10fe55e88e070fe03a7e1acafaccdf47c4e3f8fb09f4034f8.
This is local independent construction parity, not CI, release authorization,
or operational verification. The live composition writer owns its constructor
changes and actual entrypoint tests; no ledger completion is inferred.

decoder_snapshot_review additionally owns a read-only normative readiness
consultation for the next registered-integrity boundary: exact self-digest,
cursor signature and external checkpoint preimages and trusted inputs. This is
distinct from runtime composition and cannot mutate a signing wire contract.

The resumed consolidated construction regression passes 265 tests in 7.09s
with warnings treated as errors. registry-construction-regression.json records
its exact test files and current source-candidate reference. This check excludes
the runtime composition still under its sole writer; historical wheel evidence
is not promoted. Reader evidence now records the completed source refresh and
bounded test consultation instead of listing them as pending.

The registered-integrity readiness consultation finds no unresolved semantic
choice: root-only exclusions, signature-purpose/full-binding LP and the closed
checkpoint constructor are specified. Key/lifecycle composition remains work,
not a user decision or permission gate.

typed_value_candidate_encoding is a Terra worker with sole ownership of the
existing model codec and a new feature-local candidate-encoding test. It supplies
the missing native-to-source-body API through the existing bounded emitter,
followed by real source validation/native materialization/exact roundtrip. It
must not fabricate validated wrappers or grant integrity/persistence authority.
This is disjoint from registry_runtime_composition's capability/store/config
files. Root owns tests and source-package refresh: its source change will
invalidate the current decoder candidate and associated parity evidence until
regenerated. Current 265-test evidence remains the pre-encoding checkpoint.

The first runtime/candidate checks expose three construction-test failures:
two provider cases build an invalid empty decoder-snapshot pin tuple before
their intended entrypoint, and the candidate node-limit case expects a direct
message although the correct rejection is wrapped by body/codec errors. Root
classifies these as Not applicable / changes_required / verification, with
finite test corrections assigned to the existing sole writers. Results: provider
2 failed, 7 passed, 32 deselected in 63.94s; candidate/codec/reader 1 failed,
103 passed in 6.56s. Neither failure justifies weakening a production limit.

Explicit test-file Pyright also finds eight diagnostics. One new intentional
mutable-input negative needs an explicit test-only type crossing; seven other
test diagnostics remain unclassified (no clean-base proof). The production
capability's object-typed lifecycle parameter is corrected to its canonical
PrivilegedSemanticIntegrityLifecycle type. Required production scope and an
extra explicit test-file diagnostic must be reported separately; no broad
static-check success is claimed.

The corrected provider registry tests pass 5 cases in 20.20s using the real
publication verifier; the candidate boundary tests pass 3 cases in 4.65s.
Configured production Pyright plus the candidate test passes with zero errors,
and scoped Ruff passes. Frozen identities and exact results are in
runtime-candidate-encoding-candidate.json. registry_composition_review and
registry_composition_test_review are Terra read-only bounded reviewers;
registry_composition_map is Spark read-only exact callsite/caller-count refresh.
No writer owns the frozen paths during review. Root owns package/evidence work.

Candidate encoding changed one selected decoder source file. Prior candidate
metadata is preserved in publication-candidate-before-candidate-encoding.json.
The rebuilt 34-file/6086-row candidate independently reproduces all179entries;
58vectors and full-package equality pass at combined manifest
a66a7dd1e593ab791f131faebad4bfa898db455922355a030e36f1e3b60f2871.
The current registry digest is
95c1e1e980ad6d36342c109b0602d4e25885deb53f27223cd3ba8d12c69f48b1.
These are construction inputs, not issued deployment pins or ledger activation.
registered-integrity-readiness.md preserves the precise next authority boundary.

The mapper's verifier1/candidate0/reader0 production call counts and constructor
identity chain are verified by coordinator rg queries. Its wording about an
activated path and reader signatures is unsupported and rejected: configured
construction does not activate writes; the reader verifies no signatures.
The parent binding ledger now records only the supported construction outcome
and retains missing downstream production callers explicitly.

The fresh local wheel contains all1257registry JSON files byte-identically,
excludes acceptance/tests, and imports the new registry/runtime/codec owners
from its installed location. Wheel SHA256:
a44012d5c6a1bd7820d18135938e0d40c1d1ca0127b5c93a2d882efd353e365a.
The ignored prior build directory was moved aside to avoid stale package
contents. runtime-registry-wheel-evidence.json records the no-build-isolation
limitation; this is not equivalent to the complete CI package gate. The
configured identity-hygiene command exits0.

The first bounded review round yields two confirmed test-evidence actions:
exercise configured verification through the normal public production-domain
provider constructor, and compare candidate output with independent canonical
bytes using a truly verified small publication. Both are Not applicable /
changes_required / verification and assigned as one consolidated test-only
correction to their original writers. Production source remains frozen.

The correctness review's opaque-token request is unsupported: the independent
spec auditor confirms the approved boundary treats custom host builders as
trusted extensions and requires protected host composition/exact identity, not
an in-process issuer token. No untrusted request path or product-impact
scenario was demonstrated. Its zero candidate-caller P2 is also unsupported
for this explicitly bounded construction slice; that absence is already
recorded as unfinished parent integration. The same reviewer receives a
bounded scope/normative correction. The spec auditor approves the frozen
production slice with no concrete conformance gap; parent requirements remain
partial.

The corrected correctness judgment approves the frozen production slice with
no confirmed findings; the spec judgment agrees. Only the two confirmed
test-evidence actions remain in the bounded review delta.

registered_artifact_integrity is a Terra writer for a new protected integrity
owner and its own feature-local tests only. It implements the predefined
ordinary/self-digest/cursor-signature matrix from registered-integrity-readiness.md
after raw envelope selection and real native roundtrip. It must retain all
nested fields in the root preimage, use an independently supplied public key,
and keep cryptographic validity distinct from lifecycle/read/write authority.
The external checkpoint policy fails closed pending its fixed verified-context
constructor; no generic callback or omitted-check fallback is permitted. This
does not close integrity as a whole. The existing source files remain frozen
for review; root will add the new owner to explicit source closure and refresh
construction evidence after its candidate is verified.

The independent candidate literal initially had one missing JSON array close.
The writer corrected its syntax using a standalone stdlib JSON parse, without
copying candidate bytes. The affected oracle test now passes in 5.08s; the
other two real-publication tests passed in 5.12s. Public provider tests pass
five cases in 19.77s. Frozen test hashes and exact execution distinctions are
updated; the original review candidate is retained before the evidence delta.
The same test reviewer evaluates only its two finite actions.

Root construction inspection of the new integrity owner rejects dynamic
cryptography imports and its initial raw32 signature digest member. A bounded
spec consultation confirms the digest LP member is 64 lowercase hexadecimal
ASCII/UTF-8 characters, consistent with the declared sha256 grammar. This is
a determinate conformance correction, not a new wire choice. The worker uses
ordinary typed imports and must reject a signature over the raw32 alternative.

All three bounded reviews now approve the runtime/candidate construction and
its finite evidence delta. runtime-candidate-encoding-review.json records exact
candidate identity and separate judgments. No parent requirement or overall
registry publication is closed by these local approvals. Registered integrity
is the sole active implementation slice; downstream production emission,
activation, ledger, replay, retrieval and final gates remain explicit.

Initial integrity verification rejects the worker's completion report. All four
tests fail before the intended behavior because the fixture omits the declared
external checkpoint-preimage dependency and its closure. Configured Pyright
resolves ordinary cryptography imports correctly; it finds 88 test typing
diagnostics, contradicting the worker's environment-blocker explanation. Root
also finds the self-digest test never invokes the verifier and does not prove
the mutations claimed by its name. These are confirmed bounded verification
actions. The original writer owns one consolidated closure/typing/stage and
integrity-mutation correction. No source package refresh or integrity approval
is claimed until that complete candidate passes.

The activation mapper exceeded its bounded exploration budget and was stopped.
Its final output mixes helper callsites with reachable production entrypoints,
contains malformed paths and misidentifies writer_admission_memory_id as a
method. It is not authoritative activation evidence. Root independently
confirmed current(), require_current(), transition() and advance_policy_epoch()
in writer_admission.py; transition's drain scan belongs to the existing
delivery-migration protocol. No observation activation owner exists yet. Future
activation work must inspect the actual method bodies and full-snapshot CAS
contract, not transplant migration authority or rely on this mapper response.

Coordinator follow-through found a dead constructor spy in the approved bad-pin
test: capability locally imports the writer class, so patching its global name
does not intercept that call. Root, now sole writer for this test-only line,
patches the canonical writer_admission module instead. The corrected case
passes in 9.62s and retains the no-admission-record assertion. Production is
unchanged; exact prior review candidate is preserved and this one-line evidence
correction is sent to the same test reviewer.

## Registered Integrity Verified And Checkpoint Construction Started

Root combined integrity/reader verification passes 91 cases in 5.93s with
warnings as errors. Scoped Ruff and configured two-file Pyright pass. Spec and
correctness approve the frozen bounded implementation; test review resolves its
one historical-policy evidence action after a real two-publication test proves
original policy selection. registered-integrity-review.json records the scope.

The explicit decoder closure now has 35 files and 6265 rows. Root regenerated
all 179 entries/1255 roles; independent output agrees and all 58 vectors pass.
The new combined manifest is
39b592f761522f7570d913cf4462c5ed6a75d0abd2699f539dd1b0e64fa674bd.
The prior candidate and wheel evidence are preserved as historical artifacts.

A bounded spec consultation confirms that durable replay/lifecycle/activation
authority is still absent. The next Terra sole-writer slice owns only
observation_checkpoint_integrity.py and its feature-local test file: fixed
preimage construction and non-authorizing cryptographic verification. External
policy dispatch remains fail-closed until protected replay supplies authority.
Root owns tests, documents, source-closure regeneration and package verification.
One construction pass and one consolidated correction are budgeted; no changes
to public schemas, legacy bytes, thresholds or runtime authorization are allowed.
This prepares the explicit checkpoint policy without pretending supplied typed
values establish full replay or lifecycle trust. Ledger integration remains the
next dependent owner, outside this registry prerequisite packet.

The refreshed 35-file local wheel passes all 1257 packaged JSON byte comparisons
and installed integrity/configuration/capability/provider imports. The first
smoke command named a nonexistent provider module; the corrected canonical
import passes without rebuilding. Wheel SHA:
f2122437570637faccebfaefe3272ebe7e10e41d8fc82d4575f7505134305b45.
Configured identity hygiene and diff checks pass. These are local checks with
no build isolation, not the complete CI packaging contract.

The checkpoint worker's first delivery proves only digest-mismatch/one head
join, so root rejects freeze readiness: positive cryptography and independent
preimage bytes are required by the predefined matrix. The sole writer completes
one finite evidence correction using explicitly constructed test CTV fields,
real Ed25519 and typed invocation helpers; root runs the completed tests.

Coordinator directly inspected the next activation boundary: MemoryPlaneService
read_write_snapshot() at service.py:170 and conditionally_write_records() at
:188 provide the verified full revision guard. Writer admission transition() is
certified delivery migration; advance_policy_epoch() is specifically accepted
only with atomic projection publication by SemanticGovernedWritePolicy. Neither
is observation activation. The next ledger packet must add the explicit typed
admission/binding activation coordinate and recognized activation/head batch,
scan complete drained inventory under expected_write_revision, and disable old
mutation grammars after activation. Existing source-local terminal/group writes
remain operational before that explicit cutover. This map uses actual symbols
and is not a claim that activation already exists.

The fixed checkpoint helper's positive independent literal CTV/LP/Ed25519 test
passes in6.31s. Matrix consultation confirms that the initial test still lacks
required crypto, coordinate, stage and cap families. This is completion of the
already specified evidence action, not another speculative product round.
Production remains frozen while the sole writer finishes those test families.
The 36-file/6444-row source package is regenerated, full179entry output agrees,
and all58vectors pass at combined manifest
250c549fc2a3e6d4510b5a1c2dbe2dc67fc47898ed09b2456c37f51bb0c75be1.
The36-source wheel is
898031380b1524bf80d110e94e91282a052f75e3e1fb6e29351951ec89c4378c;
all1257JSON bytes and installed imports match. Scope remains local construction.
Linked ../observation-ledger/implementation.plan.md prepares actual activation,
append, replay and public read work without converting this prerequisite plan.

The completed checkpoint matrix passes five cases in6.87s, with root scoped Ruff,
configured two-file Pyright, identity hygiene and diff checks. It covers real
independent CTV/LP/Ed25519 positive, signature/digest/purpose/raw32/body changes,
all fixed joins, exact/one-below caps through a real forwarding spy, and wrong
selected coordinate before encoding. checkpoint-integrity-candidate.json freezes
production/test hashes; three independent roles review this coherent pure slice.

All three independent roles approve the frozen pure checkpoint slice with no
remaining confirmed findings; checkpoint-integrity-review.json records its exact
candidate and exclusions. Registry/helper engineering now supplies verified
construction, but protected runtime activation/replay remain open. Continue in
the linked ledger implementation packet; no production signatures or external
user decision are needed for the next deterministic integration work.
