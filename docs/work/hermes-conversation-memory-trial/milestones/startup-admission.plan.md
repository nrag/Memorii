# Installed Hermes Startup And Completed-Turn Admission

- Parent: `docs/work/hermes-conversation-memory-trial/implementation.plan.md`
- Status: `complete`
- Base revision: `3cfc1efc521c98ba4c8dfa048af8546cf4ec0d3e`
- Candidate identity: recorded by the parent candidate manifest

## Scope

Own HCM-01, HCM-02, the admission portion of HCM-05, and the startup/status
portion of HCM-06. One first-party factory consumes strict installation-bound
local authority. A complete Hermes user/assistant pair becomes one governed
two-child operation with replay-stable identity. Missing, incomplete,
substituted, expired, wrong-home, or co-installed-factory input fails closed.

## Result

The package declares one Hermes provider and one first-party service factory.
The development connector declares no service factory. The operator CLI
authorizes local Level 2 only after installed resource verification. The
factory repeats authority verification, activates the observation ledger, and
constructs the current Bootstrap V3 provider and completed-turn runtime.
Completed-turn admission binds installation, session, authenticated author,
task namespace, ordered transcript, ordinal, and exact source bytes. Exact
redelivery is idempotent; changed and incomplete transcripts deny before model
or storage work.

The product integration continues through semantic commit and protected recall,
so this milestone no longer has the earlier composition blocker. The historical
blocker was resolved by using one current Bootstrap V3 material boundary with a
closed local Level 2 execution-authorization arm.

## Evidence

- Current-runtime and Hermes focused unit suite: `54 passed`.
- Product integration: `1 passed in 545.22s`.
- Ruff, `py_compile`, and `git diff --check`: pass.
- Windows operational proof: the installed image initialized the sole provider,
  committed a live OpenAI-backed project-owner assertion, exposed the exact
  runtime-context projection through `memorii-hermes inspect`, and recalled it
  after a same-volume restart.

## Next Action

Use the parent WorkPlan's completed Level 2 evidence in the pull-request review.
