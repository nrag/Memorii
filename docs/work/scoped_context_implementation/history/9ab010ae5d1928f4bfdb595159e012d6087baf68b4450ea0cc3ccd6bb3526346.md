# Scoped Context Implementation

- Work ID: scoped_context_implementation
- Work type: implementation
- Status: active
- Coordinator: Codex main thread
- Created: 2026-09-06
- Last updated: 2026-09-06
- Parent WorkPlan: docs/work/semantic_context_activation/design.plan.md (complete design)
- Related WorkPlans: testing.plan.md; docs/work/semantic_ingestion/implementation.plan.md (M5 remains pending)
- Canonical inputs: docs/design/scoped_memory_context.md
- Expected outputs: typed scoped-context API, snapshot readers, production bindings, tests and recorded verification

## Objective And Completion Contract

Implement SMC-R01 through SMC-R10 through the approved public roots. Satisfy
.agents/PLANS.md implementation completion contract, focused and applicable
local workflow gates, coherent three-role review and fresh final review.
Record remaining_validated_p1_p2: [] only after verified closure. No live or
hosted CI success may be inferred from local evidence.

## Baseline And Scope

Base HEAD: b4f6c24b091a28bd3d1f65102c742478fc7276b3.
Approved design SHA256: f43d2cca76a57776cc2223ec1e9d413cb0deb6e94a5981263b0b43deae04386e.
Initial tracked tree clean; untracked canonical design and its evidence belong
to the preceding completed task and are preserved. No unrelated changes seen.
Implement additive read API only. M5 scheduling is preserved guidance; this
operation does not implement deployment acceptance, supply POLICY/TRACEABILITY,
activate hosts, add durable indexes/events, or implement execution control.

## Constraints And Governing Sources

AGENTS.md precedence applies: memorii_spec, storage details, event_model,
IMPLEMENTATION_RULES, scoped_memory_context and semantic_temporal_retrieval.
Preserve all six domains, candidate/commit separation, canonical lifecycle,
provenance, half-open time, oracle isolation and existing prefetch behavior.
No canonical migration. Rollout requires explicit invocation and provisioned
read authority; rollback removes invocation and drains in-flight requests.
Observability uses sanitized typed outcomes, never unauthorized data.

## Independent Requirement Reconstruction And Validation

| Requirement | Observable proof | Owner | State |
| --- | --- | --- | --- |
| SMC-R01 | Explicit mandatory IDs survive ranker outages and empty query | assembler, both roots | specified |
| SMC-R02 | Exact namespace grants, process-local identity, pre-read and final-release denial | authority and service | specified |
| SMC-R03 | All records, decoded catalogs and evidence come from one detached snapshot | memory plane and evolution repository | specified |
| SMC-R04 | Optional semantic/episodic ranking is local and scope-first | transient index | specified |
| SMC-R05 | Full purpose/temporal admission and canonical abstention, no execution dispatch | query guard and runtime | specified |
| SMC-R06 | Whole mandatory failure and shared optional content limits, capped omission IDs | schemas and assembler | specified |
| SMC-R07 | Exact source/provenance closure, scope agreement and receipt | eligibility and output | specified |
| SMC-R08 | Old provider construction/prefetch unchanged | factory and adapters | specified |
| SMC-R09 | Request-local index only, no write or persisted alternative truth | index and store | specified |
| SMC-R10 | Explicit factory/filesystem/Hermes forwarding, no alternate readers | composition roots | specified |

## Milestone

One coherent vertical API slice: contracts, authority, snapshot assembly and
real roots are inseparable acceptance. Tests are owned by linked testing plan.
Progress: readiness complete; construction active; verification pending;
coherent and final review pending. Parent M5 requirements remain partial and
are not closed by this slice.

## Identity, Change And Authority Ledgers

Approved design identity inventory is the initial durable symbol ledger.
New modules: core/scoped_context/{contracts,authority,index,service}.py.
Existing owners: provider service/factory, filesystem bundle, Hermes adapter,
memory-plane service, evolution state repository. No registry/golden/pin or
persisted schema changes planned. New behavioral identifiers must pass the
field-aware gate; no coordinate exemptions. Public schemas are new closed
contracts, not alterations to persisted records.
Testing plan owns test/workflow/timing changes and their authority chains.
This plan owns product/current-state documentation; completed design bytes stay
frozen. Live diff reconciliation and source hashes required before review.

## Production Entrypoint Bindings

Spark preflight reconciled in production-entrypoint-preflight.md before writer starts. Required chain: filesystem/Hermes -> factory -> provider
retrieve_context -> authority.resolve -> one memory snapshot -> assembler and
snapshot-only runtime -> authority.authorize_release -> typed outcome.
Missing authority explicitly denies; no prefetch substitution permitted.

### Construction Binding Update

2026-09-06 implementation update: `ProviderMemoryService.retrieve_context`
resolves the separately injected authority before the single
`MemoryPlaneService.read_snapshot` call and authorizes release after assembly.
`build_provider_memory_service_from_env` and
`FilesystemStorageBundle.build_provider_memory_service` pass the authority to
that owner. `HermesMemoryProvider.retrieve_context` is the non-test production
caller: it forwards the typed request and opaque ingress directly to
`ProviderMemoryService.retrieve_context`, with no grant construction or
prefetch fallback. The filesystem returned provider exposes the same canonical
method after authority injection. `tests/integration/test_scoped_context_production_binding.py`
proves both real roots release an authority-bound item through that chain.
This establishes the local composition binding for SMC-R10; exhaustive
owner-stripping and hosted-CI evidence remain owned by the testing plan.

## Gates, Evidence And Known Failures

Repository .venv Python 3.12 is the local runtime; CI runtime matrix is recorded
in gate inventory. Required local static gates: ruff, scoped pyright, identity.
Focused tests precede broad affected workflow commands. Testing plan owns exact
collection/timing/command map. Hosted Linux/Python 3.11 parity is not claimed.
No known product gate failure yet. No production implementation evidence yet.

## Delegation And Cost Ledger

| Task | Role/tier/ownership | Rationale and output | Status |
| --- | --- | --- | --- |
| impl_paths | Spark code-mapper/read-only | exact owner/binding preflight | complete |
| test topology consultation | Terra test_reviewer/read-only | validate attack matrix before risky code | complete; refinements in testing plan |
| scoped writer | Terra worker/sole product+test writer | coherent API and roots | active |

## Decisions, Review And Limits

User explicitly authorized implementation of the approved bounded design.
No semantic deviation authorized. Stop for unresolved material semantics;
otherwise resolve routine choices against canonical owners. Budget: one coherent
review plus at most two root-cause correction batches, then fresh final review.
Unresolved external decisions are blockers, not reasons to invent defaults.

## Next Action

Verify the frozen candidate with workflow-derived local gates and the independent review cohort.

## Outcome And Retrospective

Pending implementation and evidence.


2026-09-06 readiness: 61 baseline factory/visibility/retrieval tests passed in
7.29 seconds using .venv Python 3.12.14, pytest 9.0.3 with warnings-as-errors.
Spark ambiguity claims were unsupported by the frozen design and reconciled.
Matrix review refinements accepted in testing plan; no semantic blocker.


Construction checkpoint: sole writer has additive roots/contracts/authority and
snapshot scaffolding. It reports 40 existing compatibility tests passing; exact
commands/results remain its owned evidence until final report. Not a review
candidate: structured provenance/lifecycle closure and both-root matrix remain.
Coordinator inspected identity-key, byte-budget and typed-error boundaries and
sent construction feedback. No approval or milestone completion is inferred.


Writer handoff: scoped_writer stopped with an incomplete scaffold, not a valid
milestone closure. Coordinator retained the full objective and assigned fresh
Terra scoped_semantics as sole writer for complete runtime behavior and focused
proof. Root wiring remains; exhaustive root acceptance/timing follows runtime
completion under the same WorkPlan. No two writers are active. This changes
delegation granularity, not approved scope or completion criteria.


Coordinator integrity checkpoint: semantic writer's completion report was not
accepted. Direct code inspection found provenance source/inner-scope closure
absent from the common eligibility predicate, generic expired/invalidated state
not checked, and only direct decision evidence assembled. Focused real structured
answer/historical proof was also absent. This is unfinished assigned construction,
not an approved candidate or a review-remediation round. Sole writer continues
that bounded family; testing writer remains read-only until runtime completion.


## Candidate Freeze Checkpoint

Construction is complete. All writers are stopped. The current root proof reports 109 integration cases passing; broad local gates and coherent/final independent reviews are pending. Historical construction statements above are superseded by this checkpoint. Source-family findings have determinate corrections and focused proof in source-proof-report.md and root-proof-report.md, pending independent review. Sole writer handoffs were scoped_writer -> scoped_semantics -> scoped_tests -> source_tests -> root_tests; each is complete/inactive. All are Terra workers, chosen for bounded runtime or test families, with no overlapping writes. Spark gate mapping and Terra provenance consultation are complete. The coordinator owns gate processes and evidence. No hosted CI or operational verification is claimed.
