# Scoped Context Verification

## Current Progress

All selected broad local gates passed, including all6unit shards (3548passed,
2existing skips) and timing merge. The two-file omission-validator correction
is imported and frozen in final-source.json. Its scoped tests, static analysis,
package and source-fingerprint checks are running; fresh final review follows.
No remaining confirmed runtime P1/P2. Parent M5 is still pending.

- Work ID: scoped_context_verification
- Work type: testing
- Status: active
- Coordinator: main thread
- Created / updated: 2026-09-06
- Parent: implementation.plan.md
- Related: ../semantic_context_activation/design.plan.md
- Canonical input: approved docs/design/scoped_memory_context.md
- Expected outputs: proportional tests, exact gate ownership/timing, independent review
- Preserved pre-correction history: history/e87dac801f475593f8f69c121b0d562d71f53c3b99f9d4ca7dd39158cc00d47a.md

## Scope And Completion

Prove R01-R10 without retiring or weakening existing tests. Complete PLANS testing
contract with exact source/evidence identity and closed required findings.
Product semantics belong to implementation plan; this plan owns testing topology,
workflow/timing authority, verification and evidence limits. No live/CI success
from fake artifacts, no parent M5 closure from this bounded slice.

## Portfolio And Failure Signals

| Family | Level/owner | Failure detected |
| --- | --- | --- |
| Request/output/grant contracts |8fast unit tests in shard5 plus root negatives | malformed enum/identity/budget accepted; forged grant reads |
| Exact authority and lifecycle | filesystem/Hermes integration | wrong labels/namespaces, revocation/expiry/restart disclose data |
| Mandatory and optional budgets | unit plus roots | truncation, optional outage loses mandatory, duplicate charging |
| Scope/provenance/owned decode | root source matrices | invalidated/missing/ungrounded source, inner-scope bypass, malformed crash |
| Structured temporal |36admission pairs plus exact current/history/interval/anchor positives | unsupported execution dispatch, wrong claim, no-match/ambiguity conflation |
| Snapshot and failure families | real roots + barriers/process | second/live read, changed revision, writes, retries, untyped failure |
| Selected claim-as-evidence | multi-valued typed dependency fixture, both roots | combined duplicate IDs, false item/UTF8 overflow |
| Compatibility | existing provider/factory/shards and recapture | changed old construction/prefetch behavior |
| Workflow ownership | static_tooling and CTV exact aggregate tests | missing dedicated job/needs/env/success guard |
| Identity | existing owned surface/alias/nested/exception mutation portfolio | planning coordinates escape valid behavioral fields |

Test fixtures use real canonical records, validators, roots, runtime and authority.
Only named dependency faults and the local analyzer delegate seam are injected;
no mocked assembler/runtime proof. Interval/admission fixtures prove supported
runtime behavior, not lexical language coverage. Exact selected IDs and independent
byte calculation avoid tests that merely assert nonempty output. Legacy unclassified
claims are not valid CURRENT dependencies; test fixtures use explicit truth semantics.

## Timing And Retention

Retain all existing cases. No new slow subprocess/evolution case in unit tier.
145scoped integration cases plus8unit cases =153focused tests,19.48seconds in
scratch; main153+changed workflow assertion=154passed in19.61seconds. Dedicated
integration budget60seconds, job timeout10minutes. New8unit nodes have measured
manifest entries; original unmeasured65nodes remain baseline, not invented timing.
Generic inventory3550nodes in6file-granular shards. Corrected unit inventory is
unchanged; feature file and changed CTV file both belong to shard5. All other
shards passed and retain unchanged behavior evidence; exact shard5 and timing
merge rerun. Runtime results/timing files remain evidence, not silent replacement
of frozen duration estimates. Local longest shard exceeds hosted timeout; no
hosted headroom claim. Existing optional asset and unstructured-prompt skips are
recorded separately; no scoped test is skipped.

## Authority And Gate Map

Tests -> durations/owner budgets -> exact job -> Unit Tests required aggregate.
New job scoped-context-integration uses exact root file, warnings-as-errors and
no cache. Both existing workflow-contract owners now assert new needs/env/guard;
CTV checker pins remain byte-identical. gate-inventory.json holds every current
workflow job/matrix and selection rationale. No dependency versions/thresholds,
warning policy or fixture authority weakened. Exact executed commands/logs are
recorded under gate-results-initial and gate-results, tied to candidate manifests.

## Review, Known Failures And Limits

Initial matrix consultation completed before risky code. Source consultations
confirmed defects/evidence gaps reconciled in consultation-reconciliation.md.
Scratch writer fixed all assigned proof gaps; main directly inspected/imported
only bounded files. The stale CTV aggregate assertion is an in-scope conformance
failure, corrected and rerunning; no baseline exclusion. Scratch interrupted
full CTV run is not passing evidence and not a diagnosed defect. Main full job
and owning shard supply its completion proof. No PR exists for current branch;
no hosted run/event/merge SHA or live-provider/agent evidence is claimed.
Coherent and fresh final independent test reviews remain required.

## Delegation And Next Action

Sequential Terra writers own overlapping artifacts; Spark mapped inventory and
entrypoints; Terra matrix/source reviewers are read-only. Detailed cost/ownership
ledger is in parent plan. No writer active on frozen reviewed artifacts.

Exactly one next action: complete frozen-candidate workflow verification and independent review, preserving exact test/evidence ownership.

## Outcome

Active verification; no testing-operation completion claim yet.
