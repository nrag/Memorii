# Scoped Context Verification

- Work ID: scoped_context_verification
- Work type: testing
- Status: active
- Coordinator: Codex main thread
- Created: 2026-09-06
- Last updated: 2026-09-06
- Parent WorkPlan: implementation.plan.md
- Related WorkPlans: ../semantic_context_activation/design.plan.md
- Canonical inputs: docs/design/scoped_memory_context.md, frozen SHA in implementation plan
- Expected outputs: feature tests, exact path-to-gate map, measured timing and review

## Objective, Scope And Completion

Prove all ten scoped-context requirements without weakening existing suites.
Complete testing contract in .agents/PLANS.md; code semantics owned by linked
implementation. No test retirement or new acceptance authority. One sole writer
owns product and overlapping tests. No runtime success or hosted CI claim from
local tests. One coherent topology review plus bounded corrections and final
implementation cohort evaluates this testing scope.

## Portfolio And Failure Matrix

| Family | Level / intended owner | Distinct failure signal |
| --- | --- | --- |
| Closed request/result schemas | fast unit / generic shards | invalid enum, duplicate ID, UTC mismatch or nonempty failure accepted |
| Grant resolution | fast unit / generic shards | forged, wrong-label, exact-null, expired or revoked handle reads |
| Mandatory/budgets | fast unit / generic shards | truncation, optional outage removes mandatory, byte accounting mismatch |
| Optional index | fast unit / generic shards | scope-leaked statistics, nondeterministic tie, unsupported scorer success |
| Snapshot/roots | integration / dedicated scoped-context job proposed | live fallback, extra read, wrong clone, forwarding bypass |
| Provenance/lifecycle | unit plus root matrix | missing source/inner scope mismatch or expired claim emitted |
| Structured temporal | 18-pair matrix / integration | unsupported pair reaches execution or ambiguity produces claim |
| Failure/revocation | integration using fake clocks/events | final release leaks data, backend corruption becomes no-match, retry |
| Compatibility | existing provider suites and roots | default construction/prefetch behavior changes |
| Identity | existing gate plus mutations | new durable schema/name coordinates accepted; legitimate data rejected |

Both filesystem and Hermes must cover R01-R10 with real adapters and validators.
No mocks of assembler, authority or canonical validation. Fault injection only
at exact dependencies; expected returned envelopes and zero-read/write spies
are the independent oracle. Full-root repeated construction, restart and
exhaustive pairs belong to integration, not unit. Deterministic synthetic
records are not ingestion acceptance authority. High-risk tests must demonstrate
failure when corresponding forwarding/authority/reader edge is stripped.

## Baseline, Timing And Gate Map

Existing generic unit inventory tests/ci/unit-shards.json has six shards and
720-second target; collector assigns new files automatically. Existing timing
manifest must receive actual measured new unit nodes, never default timing at
closure. New focused unit file budget 10 seconds; new integration file budget
60 seconds initially, measured before closure. Current no-new-feature baseline
is zero feature nodes. Existing owner count/runtime inventory pending.
Path map and exact workflow commands will be recorded in gate-inventory.json.
New integration required job can own only this unique matrix; avoid duplication.
CI Python 3.11/Linux differs from local Python 3.12/macOS, explicitly recorded.

## Identities And Authority Chain

Behavioral files: test_scoped_context_activation.py and
 test_scoped_context_production_binding.py. No new coordinate-named IDs.
Tests -> measured durations -> unit shard plan or dedicated job -> required
aggregate; workflow contract verifies new job argv/ownership if added.
Existing normative ingestion/golden/registry pins stay unchanged.
Retention: retain every existing case; no obsolete artifacts identified.

## Delegation, Evidence, Decisions And Review

Terra test_reviewer consults frozen proposed matrix before risky implementation;
Terra sole writer implements. Spark inventory may map exact job ownership.
No tests run yet. No known failure. No hidden semantic choice may be invented.

## Next Action

Verify the frozen candidate with workflow-derived local gates and the independent review cohort.

## Outcome

Pending.

## Writer Construction Evidence

2026-09-06 focused construction run used `.venv` Python 3.12.14:
`python -W error -m pytest -q tests/unit/core/test_scoped_context_activation.py
tests/unit/core/test_provider_factory.py tests/unit/core/test_filesystem_storage_bundle.py
tests/unit/integrations/test_hermes_identity_lineage.py -p no:cacheprovider`:
43 passed in 5.69 seconds. `ruff check` over changed product and focused test
paths passed. This establishes only early service/root compatibility and the
new unit nodes; no integration job, measured timing inventory, full both-root
matrix, or required aggregate wiring exists yet.


## Pre-Coding Matrix Consultation Resolution

Terra matrix_review requested determinate evidence refinements; all accepted
with the distinction that authority recreation/process restart invalidates
handles, whereas reopening a store alone does not revoke a live authority.

Both root triggers are parameterized: FilesystemStorageBundle's returned
provider.retrieve_context, and HermesMemoryProvider.retrieve_context. For each
root, prove the following observable case, without replacing its canonical
owner: R01 mandatory survives scorer outage; R02 missing/forged/revoked authority
has no reads/data; R03 capture then mutate retains clone and revision; R04 only
allowed semantic/episodic IDs ranked; R05 all 18 purpose/kind combinations and
no execution dispatch; R06 byte/item boundary and mandatory-overflow emptiness;
R07 full source closure and receipt; R08 old prefetch retained/no substitution;
R09 no writes and rebuild from next snapshot; R10 exact forwarding chain.

Binding mutations remove factory, filesystem and Hermes authority/ingress
forwarding separately. Each must fail closed via empty denial or unavailable
at the actual boundary; spies prove no prefetch/live-reader fallback. Snapshot
reader removal/fault cannot be silently replaced. Mutations are temporary
fixtures at dependency seams, never test-only branches in production.

Both roots cover one-attempt backend/decode faults -> UNAVAILABLE, scorer fault
-> SCORER_UNAVAILABLE, structured dependency fault -> STRUCTURED_UNAVAILABLE,
and unexpected bug -> propagated exception with no success. Expiry equality,
revocation/reprovision before release use fake clock and barriers, never sleep.
Fresh JSONL reopen with a new authority rejects old handles; persisted bytes
remain unchanged. Legacy omitted/explicit-null rows require the identical
finite all-null allowlist. Record installed ICU/fallback tokenizer identity.

All refinements are required before construction acceptance. No design change,
new product defect or externally owned choice is inferred by this consultation.


Baseline inventory verification passed before feature tests: 3,542 collected,
3,477 measured; six shard counts [17,496,844,740,864,581], estimates
[601.629,583.933,583.932,583.933,583.933,583.932] seconds under 720-second
budget. Command from memorii/: ../.venv/bin/python -m memorii.tools.test_shards
verify --config tests/ci/unit-shards.json, exit 0. Existing unmeasured nodes
are baseline; every newly added node must have actual timing before closure.
Local: macOS 26.6.1 x86_64, Python 3.12.14, pytest 9.0.3, Pydantic 2.13.4,
Ruff 0.15.21, Pyright 1.1.411, PyICU 2.16.2. Pinned historical compatibility
commit f76850fc45f09d21a40b5a7302d173ce642ec9d6 exists locally.


Actual feature test paths follow existing repository conventions:
memorii/tests/unit/core/test_scoped_context_activation.py and
memorii/tests/integration/test_scoped_context_production_binding.py.
Initial dedicated scoped-context-integration job is wired to Unit Tests; its
complete matrix/structure/timing proof remains pending. A test writer is
preparing read-only while sole production writer completes semantics, and
will receive exclusive write ownership only after that writer stops.


Temporal matrix construction complete: 36 real-root cases (2 roots x 3 purposes
x 6 temporal kinds), 36 passed in 5.32 seconds. Only local analyzer delegate
output is supplied; guard/runtime validation stays real and execution-dispatch
spy stays zero. This proves closed dispatch/admission, not natural-language
coverage for the lexical analyzer (which does not emit INTERVAL natively).
Actual current/historical lexical positive fixture remains separate proof.
Next bounded construction family: deterministic authority release races/reopen.


Authority lifecycle integration family completed: 8 cases passed in 13.63s
(expiry equality, before-release barrier revoke/reprovision, post-release revoke,
and fresh-process JSONL/authority behavior through both roots). No sleeps.
Source/lifecycle construction consultation confirmed four P2 families; see
construction-findings.md. Sole writer owns one common invariant correction
with both-root proofs. This is not final approval.


## Candidate Freeze Checkpoint

Construction is complete. All writers are stopped. The current root proof reports 109 integration cases passing; broad local gates and coherent/final independent reviews are pending. Historical construction statements above are superseded by this checkpoint. Source-family findings have determinate corrections and focused proof in source-proof-report.md and root-proof-report.md, pending independent review. Sole writer handoffs were scoped_writer -> scoped_semantics -> scoped_tests -> source_tests -> root_tests; each is complete/inactive. All are Terra workers, chosen for bounded runtime or test families, with no overlapping writes. Spark gate mapping and Terra provenance consultation are complete. The coordinator owns gate processes and evidence. No hosted CI or operational verification is claimed.
